IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 1 Trans VG++: End-to-End Visual Grounding with Language Conditioned Vision Transformer Jiajun Deng, Zhengyuan Yang, Daqing Liu, Tianlang Chen, Wengang Zhou, Senior Member, IEEE, Yanyong Zhang, Fellow, IEEE, Houqiang Li, Fellow, IEEE, Wanli Ouyang, Senior Member, IEEE Abstract—In this work, we explore neat yet effective Transformer-based frameworks for visual grounding. The previous methods generally address the core problem of visual grounding, i.e., multi-modal fusion and reasoning, with manually-designed mechanisms.
Such heuristic designs are not only complicated but also make models easily overfit specific data distributions. To avoid this, we first propose Trans VG, which establishes multi-modal correspondences by Transformers and localizes referred regions by directly regressing box coordinates. We empirically show that complicated fusion modules can be replaced by a simple stack of Transformer encoder layers with higher performance. However, the core fusion Transformer in Trans VG is stand-alone against uni-modal encoders, and thus should be trained from scratch on limited visual grounding data, which makes it hard to be optimized and leads to sub-optimal performance. To this end, we further introduce Trans VG++ to make two-fold improvements. For one thing, we upgrade our framework to a purely Transformer-based one by leveraging Vision Transformer (Vi T) for vision feature encoding. For another, we devise Language Conditioned Vision Transformer that removes external fusion modules and reuses the uni-modal Vi T for vision-language fusion at the intermediate layers. We conduct extensive experiments on five prevalent datasets, and report a series of state-of-the-art records.
Index Terms—Transformer Network, Visual Grounding, Vision and Language, Deep Learning (cid:70) 1 INTRODUCTION measures the similarity between region embedding and expression embedding by cosine distance. The early one VISUAL grounding, which aims to localize a region restage approach, FAOA [7], encodes the language feature ferred to by a language expression in an image, is a core vector to each spatial position of vision feature maps by technology to bridge the natural language expression delivdirectly concatenating them. In general, these attempts are ered by human beings and visual contents in the physical efficient, but lead to sub-optimal results, especially when world. The evolution of this eachnique is of great potential it comes to complicated language expressions [10], [11].
to promote vision-language understanding, and to provide Following studies have proposed diverse architectures to an intelligent interface for human-machine interaction. Exameliorate their performance. Among two-stage methods, isting methods addressing this task generally follow twomodular attention network [12], various scene graphs [13], stage or one-stage pipelines shown in Figure 1. Specifically, [14], [15], and multi-modal tree [10], [16] are designed two-stage approaches [1], [2] first generate a set of region to improve multi-modal relationships modeling. The oneproposals, and then take visual grounding as a natural stage method [11] also explores query decomposition and language object retrieval problem [3], [4] to find the best proposes a multi-round fusion mechanism.
matching region given language expressions. Differently, Despite their effectiveness, these sophisticated fusion or one-stage approaches [5], [6], [7] perform vision-language matching modules are built on pre-assumed dependencies fusion at the output of the vision backbone network and the of language expressions and visual scenes, making the modlanguage model. Then, they make dense predictions with els easily overfit specific scenarios, such as certain query a sliding window over pre-defined anchor boxes, and keep lengths or objects relationships. Meanwhile, these mechathe box with the maximum score as the final prediction.
nisms limit the plenitudinous interaction between vision Multi-modal fusion and reasoning is the primary proband language contexts, which also hurts the performance lem in visual grounding. The early two-stage and one-stage of visual grounding algorithms. Besides, even though the methods address multi-modal fusion in a simple way. Contarget is to localize the referred region, most of the previcretely, the pioneer two-stage method, Embedding Net [9], ous methods achieve this target in an indirect way. They generally define surrogate problems of language-guided • J. Deng, W. Zhou, Y. Zhang and H. Li are with University of Science and candidates matching, selection, and refinement, following Technology of China. W. Zhou, Y. Zhang and H. Li are also with Institute the common practice of image-text retrieval and object of Artificial Intelligence, Hefei Comprehensive National Science Center.
detection. Therefore, extra efforts have to be devoted to (email: dengjj@ustc.edu.cn, zhwg@ustc.edu.cn, yanyongz@ustc.edu.cn, lihq@ustc.edu.cn) obtain candidates, including region proposals [2], [17], [18] • Z. Yang is with Microsoft. (email: zhengyuan.yang 13@gmail.com) and pre-defined anchor boxes [7], [11]. Since these methods’ • D.Liu is with JD Explore Academy. (email: liudq.ustc@gmail.com) predictions are made out of candidates, their performance • T. Chen is with Amazon. (email: sunnychencool@gmail.com) • W. Ouyang is with the University of Sydney and Shanghai Artificial is easily influenced by the step to generate such candidates Intelligent Laboratory. (email: wanli.ouyang@sydney.edu.au) and by the heuristics to assign targets to candidates.
• Corresponding authors: W. Zhou and H. Li.
In this work, we explore an alternative approach in 2202 nu J 41 ]VC.sc[ 1 v 91660.6022:vi Xra IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 2 A white cushion chair A white cushion chair Language Encoder Language Encoder with a grey pillow on it with a grey pillow on it Region-Expression Vision-Language CNN CNN Matching Fusion Best matching Dense Prediction with Region Proposals proposal Predictions max score (a) Two-stage pipeline (b) One-stage pipeline A white cushion chair LL aanngguuaaggee A white cushion chair Language with a grey pillow on it TT rraannssffoorrmmeerr with a grey pillow on it Transformer Vision-Language Transformer Direct Coordinates Language Conditioned TT rraannssffoorrmmeerr CNN Regression Vision Transformer ((VV iissiioonn)) Direct Coordinates Patch Embedding Regression (c) Trans VG (preliminary version) (d) Trans VG++ (advanced version) Fig. 1: A comparison of (a) two-stage pipeline, (b) one-stage pipeline, (c) our preliminary Trans VG framework, and (d) our advanced Trans VG++ framework. Trans VG performs intra- and inter-modality relation reasoning with a stack of Transformer layers in a homogeneous way, and grounds the referred region by directly regressing the box coordinates. Trans VG++ takes further steps that upgrade the hybrid architecture composed of convolutional neural networks and Transformer networks to purely Transformer-based one and remove the stand-alone fusion Transformer by introducing Language Conditioned Vision Transformer to enable fusion at the intermediate layers of a vision feature encoder. Particularly, we use Transformer (vision) to represent a stack of Transformer encoder layers built on the top of a convolutional neural network (CNN), distinguished from Vision Transformer [8], a specific model in the literature.
fusion module design, and re-formulate the prediction pro- To this end, we further introduce an advanced version cessing back to a simple regression problem. We first present framework, namely Trans VG++, to remove the stand-alone Trans VG, the preliminary version framework to address fusion Transformer, and instead performs vision-language the problem of visual grounding with Transformers. We fusion by re-using the vision feature encoder. As shown empirically show that the structurized fusion modules can in Fig.1(d), we devise Language Conditioned Vision Transbe replaced by a simple stack of Transformer encoder layers. former (LV i T) to play the role of both vision feature en The insight behind our design is that the basic component coding and vision-language reasoning. Technically, LV i T is of Transformer, i.e., attention module, is ready to estab- obtained with minimal adaptation and extra parameters, by lish intra- and inter-modality correspondence for vision integrating information of language expressions into interand language inputs, despite we neither suppose any lan- mediate layers of the plain Vision Transformer (Vi T) [8]. Two guage expression structure nor visual layout. The pipeline novel alternative strategies, i.e., namely language prompter of Trans VG is illustrated in Figure 1(c). We first feed the and language adapter, are proposed to convert uni-modal image and language expression into two sibling branches. visual encoder layers of Vi T into linguistic conditioned The Transformer built on the top of a convolutional neural visual encoder layers of LV i T. Built on a fully Transformer network and language Transformer built on word embed- architecture and waiving the external fusion Transformer, dings are applied in these two branches to model the global Trans VG++ achieves consistently better performance with cues in vision and language domains, respectively. Then, even smaller model size and less computation costs when the abstracted vision tokens and language tokens are fused compared with the preliminary version. Furthermore, pigtogether, and a vision-langauge Transformer is leveraged to gybacking on the successful Vi T series allows Trans VG++ perform cross-modal relation reasoning. Particularly, at the to be scaling up with minimal efforts. We empirically prediction step, Trans VG directly outputs 4-dim coordinates show that larger LV i T models consistently lead to betof a bounding box to localize the referred region, instead of ter Trans VG++ variants, and expect the trend to hold for making predictions based on a set of candidates. stronger Transformer based vision backbones.
We benchmark the proposed frameworks on five preva Although Transformers have been leveraged to establish lent datasets, including Ref COCO [17], Ref COCO+ [17], multi-modal dependencies, Trans VG shares the same meta Ref COCO g [18], Refer It Game [19], Flickr 30 K Entities [20].
architecture with previous two-stage and one-stage meth Our preliminary framework respectively achieves 83.38%, ods, which include two independent uni-modal feature en 59.24%, 68.71%, 70.73% and 79.10% accuracy on the test coders, a stand-alone multi-modal fusion or matching modset of these five datasets, consistently outperforming the ule, and a prediction module. Within this meta-architecture, previous two-stage and one-stage approaches. Remarkably, the parameters of vision encoder and language encoder Trans VG++ further achieves 4.99%, 8.04%, 8.32%, 3.97% and can be initialized with well pre-trained models, while the 2.39% absolute improvements over the preliminary one, primary fusion module, i.e., vision-language Transformer, is setting a series of state-of-the-art records.
left to be trained from scratch with limited visual grounding In summary, we make three-fold contributions: data. As a result, the stand-alone fusion Transformer is hard to be optimized and leads to sub-optimal performance. • We propose the first purely Transformer-based frame IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 3 work to address the problem of visual grounding, and to maximize the similarity between the positive object-query re-formulate the prediction process to directly regress pairs. The following work DBN et [32] and Similarity Net [2] the box coordinates of referred regions. show that the similarity and dissimilarity of text-region • We present an elegant view of how to capture intra- and pairs can be predicted by directly performing binary clasinter-modality context homogeneously with Transform- sification. The comprehensive region proposals provided ers, and further investigate how to remove stand-alone by the vision encoder make it easy for two-stage methods fusion modules by integrating language information to reason the relationships among objects. MMI [17] first into intermediate layers of a vision encoder. proposes to incorporate visual comparison based context • We conduct extensive experiments to validate the mer- into referring expression models, which demonstrates the its of our method, and show significantly improved significance of involving out-of-object information for visual results on several prevalent benchmarks. grounding. Matt Net [12] introduces the modular design and The preliminary version of this work is published in [21]. improves the grounding accuracy by better modeling the We have made significant improvements and extensions to subject, location, and relation-related language description.
our preliminary work. The major technical improvements Recent studies further improve the two-stage methods by can be concluded in two aspects. For the most impor- better modeling the object relationships [10], [13], [14], [15], tant point, we remove the stand-alone fusion module, and [33], enforcing correspondence learning [34], or making use enable the vision encoder to be re-used for multi-modal of phrase co-occurrences [35], [36], [37].
fusion at the intermediate layers. Besides, we upgrade the One-stage Methods. One-stage approaches [5], [6], [7], [11], hybrid architecture composed of convolutional neural net- [25] get rid of the region proposals and fuse the linguistic works and Transformer networks to a purely Transformer- context with visual features densely at each spatial position based one. To the best of our knowledge, this is the first of feature maps. The fused image-text feature maps are then fully Transformer-based framework in this field, without leveraged to perform bounding box prediction in a slidingintroducing any inductive bias. As demonstrated by ex- window manner. The early work FAOA [7] encodes the lanperiemental results, the advanced framework outperforms guage expression into a language vector, and fuses the lanthe preliminary one by a large margin, and meanwhile guage vector into the YOLO v 3 detector [38] to ground the achieves better efficiency on model size and computation referred region. RCCF [6] formulates the visual grounding costs. To facilitate further investigation, we build a bench- problem as a correlation filtering process [39], [40], and picks mark of Transformer-based visual grounding frameworks the peak value of the correlation heatmap as the center of at https://github.com/djiajunustc/Trans VG, and will make target objects. The recent work Re SC [11] devises a recursive our codes and models available. sub-query construction module to address the limitations of FAOA [7] on grounding complex queries by multi-round fusion. LSPN [41] further improves the reasoning ability of 2 RELATED WORK the one-stage grounding methods.
2.1 Visual Grounding Fusion in the Vision Encoder. Abstracting away the dis Visual grounding aims to ground a natural language de- tinctions in the fusion module, one- and two-stage visual scription onto the referred region in an image. Most ex- grounding approaches share the same meta-framework, isting approaches consist of a vision feature encoder, a i.e., single-modal feature encoders followed by an external language feature encoder, and an external vision-language multi-modal feature fusion or matching module. In this fusion module. According to the differences in the fusion study, we alternatively explore how to remove the standmodule, current visual grounding methods can be broadly alone fusion module, and re-use the vision feature encoder categorized into two directions, i.e., two-stage methods [2], (i.e., a plain Vi T [8] in our Trans VG++) to perform vision- [10], [12], [13], [14], [16], [22], [23], [24] and one-stage meth- language fusion. We vision two major advantages of the proods [5], [6], [7], [11], [25]. Two-stage methods match the posed novel paradigm. First, removing the explicit fusion language feature to the vision content at the region level, layers makes the framework efficient and light-weighted, thus requiring the vision encoder to first generate a set thus also performing better with the same amount of comof region proposals. One-stage methods densely perform putations. Second, the unified vision encoder design facilimulti-modal feature fusion at all spatial locations, waiving tates the model to benefit from the advances in computer the requirements of region proposals. In the following, we vision studies, such as stronger image classification and deliver a literature review on both of them. object detection models pretrained on large scale datasets.
Two-stage Methods. Inspired by the success of region based 2.2 Transformer object detectors [26], [27], [28], two-stage methods are characterized by first generating region proposals and then se- Transformer is first introduced in [42] to tackle the problem lecting the best matching one corresponding to the language of neural machine translation (NMT). Compared with reexpression. At the first stage, region proposals can be either current units in RNN s [43], [44] and LSTM s [45], the core generated with external modules based on super-pixels component of Transformer, i.e., attention modules, show grouping [2], [29], [30], or predicted with pre-trained object remarkable superiority in long-term sequence modeling.
detectors [10], [12], [23], [31]. The main efforts of approaches Transformer attracts increasing interests in the computer in this direction are devoted to the second stage, addressing vision community, and has made unnegligible impacts on visual grounding as text-region matching. The pioneer stud- vision and vision-language tasks.
ies [1], [9], [18] obtain good results by optimizing the feature Transformers in Vision Tasks. Inspired by the great success embedding networks with maximum-margin ranking loss in NMT, a series of Transformers [8], [46], [47], [48], [49], IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 4 Prediction Head Prediction Head Regression MLP Regression MLP Vision-Language Fusion Module ...... ......
x 4 Vision-Language Transformer Language Conditioned ... ... ...... ......
Vision Encoder Layer [REG] Linear Projection Linear Projection Linear Projection Vision Encoder Layer Transformer (vision) LL aanngguuaaggee TT rraannssffoorrmmeerr Vision Encoder Layer LL aanngguuaaggee TT rraannssffoorrmmeerr [[CCLLSS]] [[SSEEPP]] [[CCLLSS]] [[SSEEPP]] ...... ...... ......... ......... ...... ...... ......... .........
[REG] RC es NNN et LLLL aaaannnngggguuuuaaaaggggeeee EEEE mmmmbbbbeeeeddddddddiiiinnnngggg Patch Embedding LLLL aaaannnngggguuuuaaaaggggeeee EEEE mmmmbbbbeeeeddddddddiiiinnnngggg ““TT hhee bbaannaannaa uunnddeerr ttwwoo ““TT hhee bbaannaannaa uunnddeerr ttwwoo oorraannggeess..”” oorraannggeess..”” Vision Branch Vision Branch Language Branch Language Branch (Language Cond. Vision Transformer) (a) Trans VG (b) Trans VG++ Fig. 2: An overview of our proposed Trans VG and Trans VG++ frameworks. Trans VG includes four components: a vision branch, a language branch, a vision-language fusion module and a prediction head. Vision tokens, language tokens and a learnable [REG] token are put together as the inputs of vision-language Transformer for multi-modal reasoning. The output state of [REG] token is fed into the prediction head for box cooridnates regression. To extend the preliminary framework, Trans VG++ removes the stand-alone fusion module, and introduce Langauge Conditioned Vision Transformer, which enables vision-langauge fusion at the intermediate layers of a vision feature encoder.
[50], [51], [52] applied to vision tasks have been proposed. of some downstream vision-language tasks.
The infusive work DETR [46] formulates object detection Despite with similar base units, the goal of VLP is to as a set prediction problem. It introduces a set of learnable learn a generalizable vision-language representation with object queries, reasons global context and object relations large-scale data. In contrast, we focus on exploring novel with attention mechanism, and outputs the final set of Transformer-based visual grounding frameworks. Besides, instance predictions. Vi T [8] and Dei T [50] show that a Vi T has has also shown its power in other vision-language pure Transformer can achieve excellent accuracy for image tasks, while mainly as the backbone of vision encoder [63], classification. The following work Swin Transformer [51] re- [64]. They still build stand-alone blocks for multi-modal introduces inductive bias into vision Transformers by devis- fusion. We alternatively explore having the plain Vi T do ing shifted-window attention. The recent works UV i T [53] more work and removing the stand-alone fusion layers.
and Vi TD et [54] demonstrate the non-hierarchical Vi T can be directly applied as the backbone network of object detectors.
Our Trans VG++ also explores the usage of plain Vi T in 3 OUR APPROACH downstream tasks. Distinguishly, we investigate how to adapt the uni-modal Vi T for the multimodal visual ground- In this work, we present two versions of novel architecture ing task, while best preserving its power and architecture for visual grounding by leveraging the glamorous Transfor vision feature extraction. former neural networks. An overview of our proposed frameworks are illustrated in Fig. 2. The preliminary version Transformer in Vision-Language Tasks. Motivated by the framework, namely Trans VG, performs vision-language feapowerful pre-trained model of BERT [55], some researchers ture fusion with a simply stack of Transformer encoder start to investigate visual-linguistic pre-training (VLP) [56], layers, getting rid of manually designed mechanisms. The [57], [58], [59], [60] to jointly represent images and texts. advanced version framework, i.e., Trans VG++, is featured In general, the early works take region proposals and text by removing the stand-alone fusion module and introducing as inputs, and devise several transformer encoder lay- Language Conditioned Vision Transformer (LV i T), which ers for joint representation learning. Plenty of pre-training play the roles of both vision feature encoding and multitasks are introduced, including image-text matching (ITM), modal fusion. Particulary, LV i T is converted from the uniword-region alignment (WRA), masked language modeling modal Vi T [8] model with minimal adaptation and extra (MLM), masked region modeling (MRM), etc. Some recent costs. Moreover, different from previous methods that rely works [61], [62], [63], [64] further improve the model to on candidate boxes selection or refinement, both of our make use of raw image patches. Besides following the proposed frameworks introduce a learnable [REG] token, pretext tasks of BERT, MDETR [65] explores using object de- and localize the referred region by directly regressing the tection as the pretext task to help improve the performance box coordiantes with the output state of [REG] token.
IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 5 In Transformer encoder layers, each sub-layer is put into Layer Norm + a residual structure. Let us denote the input as x n , a postnormalization Transformer encoder layer computes: + FFN x(cid:48) = LN(x + F (x )), (3) FFN Layer Norm n n MHSA n x = LN(x(cid:48) + F (x(cid:48) )), (4) n+1 n FFN n + Layer Norm while the computation procedure in a pre-normalization + Multi-Head Transformer encoder layer is: Self-Attention Multi-Head Self-Attention Q K V x(cid:48) n = x n + F MHSA (LN(x n )), (5) Q K V Layer Norm x n+1 = x(cid:48) n + F FFN (LN(x(cid:48) n )). (6) 3.2 Preliminary Version: Trans VG Framework (a) Post-nomarlization (b) Pre-normalization Fig. 3: An illustration of two varieties of Transformer encoder In this subsection, we present Trans VG, the preliminary layers, including (a) a post-normalization encoder layer and version framework based on a stack of Transformer encoder (b) a pre-normalization encoder layer.
layers with direct box coordinates prediction. As shown in Figure 2(a), given an image and a language expression as inputs, we first separate them into two sibling branches, In the Sec. 3.1, we first review the background knowli.e., a vision branch and a language branch, to generate edge of Transformer networks. Then, we elaborate our corresponding feature embedding. Then, we consturct the model design of the preliminary version (Sec. 3.2) and inputs of vision-language fusion module by putting the advanced version (Sec. 3.3) frameworks. Finally, in Sec. 3.4, vision and langauge feature embedding together, and apwe introduce the training objectives of our frameworks. pend a learnable token (i.e., [REG] token). The visionlanguage Transformer homogeneously embeds the input tokens from different modalities into a common semantic 3.1 Background: Transformer space by modeling intra- and inter-modality context with the self-attention layers. Finally, the output state of [REG] Before detailing our proposed frameworks, we first deliver token is leveraged to directly predict 4-dim coordinates of a a background overview of attention modules and encoder referred region in the prediction head.
layers of Transformer neural networks [42].
Vision Branch. The vision branch includes a convolutional Attention Module. The core component of Transformer network and a following Transformer encoder. We exploit encoder layer is the attention module. Here, we take singlethe commonly used Res Net [67] as the convolutional backhead attention as an example for explanation. The input bone network, and build the Transformer encoder with a of single-head attention module is a sequence of feature tokens. Given the query tokens xq and support tokens xs , stack of 6 post-normalization Transformer encoder layers. In each encoder layer, There are 8 heads in the MHSA module, three independent fully connected (FC) layers are applied on them to generate the query embedding f Q , key embedand 2 FC layers followed by Re LU activation layers in the ding f K and value embedding f V as follows: FFN. The output channel dimensions of these 2 FC layers in the FFN are 2048 and 256, respectively.
f Q = FC(xq), f K = FC(xs), f V = FC(xs). (1) Given an image z 0 ∈ R 3×H 0 ×W 0 as the input, we exploit the backbone network to generate a 2 D feature map When support token set is the same as query token set, z ∈ RC×H×W . Typically, the channel dimension C is 2048, the attention module is named as a self-attention module. and the width and height of z are 1 of the original image 32 Otherwise, it is named as a cross-attention module. After size (H = H 0, W = W 0). Then, we leverage a 1 × 1 32 32 obtaining query, key and value embedding, the output of a convolutional layer to reduce the channel dimension of z single-head attention layer is computed as: to C v = 256 and obtain z(cid:48) ∈ RC v ×H×W . Since the input of a Transformer encoder layer is supposed to be a sequence of f Qf K 1 D vectors, we further flatten z(cid:48) into z ∈ RC v ×N v, where Attn(f Q,f K,f V ) = FC(Softmax( √ ) · f V ), (2) v d K N v = H × W is the number of input vision tokens. To make the visual Transformer sensitive to the original 2 D where d K is the channel dimension of f K , Softmax(·) is the positions of input tokens, we follow [46], [68] to utilize sine softmax function applied across the key/value embedding. position embedding as the supplementary of visual feature.
Encoder Layer. In Fig. 3, we illustrate two kinds of Trans- Concretely, the position encodings are added with the query former encoder layers, distinguished by the position to and key embedding at each Transformer encoder layer. The perform layer normalization (LN) [66]. Concretely, a Trans- visual Transformer conducts global context reasoning in former encoder layer has two main sub-layers, i.e., a multi- parallel, and outputs visual embedding f , which shares v head self-attention (MHSA) layer and a feed forward net- the same shape as input z .
v work (FFN). Multi-head attention is a variant of single-head Language Branch. The language branch is a sibling to the attention by splitting embedding channels into multiple vision branch, and it includes a language embedding layer groups. FFN is an multi-layer perceptron (MLP) composed and a language Transformer. To make the best of pre-trained of FC layers and non-linear activation layers. language models, the architecture of this branch follows IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 6 BERT [55]. Typically, there are 12 pre-normalization BASE Transformer encoder layers. The output channel dimension + of language Transformer is C = 768.
l FFN Given a language expression, we first represent each Layer Norm word ID as a one-hot vector. Then, in the language embedding layer, we convert each one-hot vector into a language Split & Drop token by looking up the token table. We follow the common practice in NMT [42], [55], [69], [70] to append a [CLS] + Language prompt tokens token and a [SEP] token at the beginning and end posi- Multi-Head + Self-Attention tions of language tokens. Different from the sine position FFN embedding leveraged in the vision branch, we make use of Q K V learnable position embedding in the language branch, and Layer Norm Layer Norm directly add them to language tokens. After that, we take Concatenate these language tokens as inputs of linguistic Transformer to Language tokens obtain the output language feature embedding f ∈ RC l ×N l, l [REG] & Vision tokens where N is the number of language tokens.
l (i) Language Prompter Vision-Language Fusion Module. As the core component in our model to fuse multi-modal information, the architecture + of the vision-language fusion module (abbreviated as V-L FFN module) is extremely simple and elegant. Specifically, the VL module includes two linear projection layers (one for each Layer Norm modality) and a vision-language Transformer (also with a Language adapt tokens + stack of 6 post-normalization Transformer encoder layers).
Given vision tokens f ∈ R 256×N v out of the vision + Multi-Head v Cross-Attention branch and language tokens f ∈ R 768×N l out of the lan- Multi-Head l Self-Attention Q K V guage branch, we apply a linear projection layer on each of Q K V them to project them into embedding with the same channel Language tokens dimension. We denote the projected visual embedding and Layer Norm linguistic embedding as g ∈ RC p ×N v and g ∈ RC p ×N l, v l where the projected feature dimension C p = 256. Then, we [REG] & Vision tokens pre-append a learnable embedding (namely a [REG] token) (ii) Language Adapter to g and g , and formulate the joint input tokens of the v l Fig. 4: An illustration of our proposed language conditioned visual-linguistic Transformer as: vision encoder layers with (i) a language prompter and (ii) a language adapter.
languagetokens g l (cid:122) (cid:125)(cid:124) (cid:123) x = [g , g 1, g 2, ··· , g N v, g 1, g 2, ··· , g N l], (7) 0 r v v v l l l layers and a linear output layer. The output of the prediction (cid:124) (cid:123)(cid:122) (cid:125) visiontokens g v head is the 4-dim box coordinates.
where g ∈ RC p ×1 represents the [REG] token. The [REG] r 3.3 Advanced Version: Trans VG++ Framework token is randomly initialized at the beginning of the training stage and optimized with the whole model. After obtaining To further drive the evolution of Transformer-based visual the input x ∈ RC p ×(1+N v +N l ) in the joint embedding grounding, we introduce Trans VG++, the advanced version 0 space as described above, we apply the vision-language framework with fully Transformer-based architecture. We Transformer to embed x into a common semantic space depict the overview of Trans VG++ framework in Fig. 2(b).
0 by performing intra- and inter-modality relation reasoning As illustrated, the main difference between Trans VG++ and in a homogeneous way. To retain the positional and modal Trans VG is that we remove the stand-alone vision-language information, we add learnable position embedding to the fusion module, and capitalize on a novel vision feature input of each Transformer encoder layer. encoder, namely Language Contioned Vision Transformer Thanks to the attention mechanism, the correspondence (LV i T). On the one hand, LV i T upgrades the hybrid vision can be freely established between each pair of tokens from feature encoder composed of Res Net and 6 following Transthe joint entities, regardless of their modality. For example, fomer encoder layers, and on the other hand LV i T enable a vision token can attend to a vision token, and it can also multi-modal fusion at its intermediate layers. Besides, the freely attend to a language token. Typically, the output state vision input of our Trans VG++ is no longer the whole of the [REG] token develops a consolidated representation images, but equally divided raw image patches.
enriched by both vision and language context, and is further Particularly, LV i T is modified from a uni-modal Vi T [8] leveraged for box coordinates prediction. model with few efforts and negligible extra computation Prediction Head. We leverage the output state of [REG] costs. Specifically, a standard Vi T model is composed of 12 token from the V-L module as the input of our prediction pre-normalization Transformer encoder layers. We denote head. To perform box coordinates prediction, we apply a them as vision encoder layers, since they are only leveraged regression block to the [REG] token. The regression block is to extract vision feature embedding, and equally divide implemented by an MLP with two Re LU activated hidden them into 4 groups. Our core modification is that we convert IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 7 the last vision encoder layer of each group into a language stage methods and anchor boxes in one-stage methods), conditioned vision encoder layer, which integrates the lan- we directly predict a 4-dim vector as the coordinates of guage feature of referring expressions into vision tokens. the box to be grounded. This simplifies the process of Motivated by the success of prompt tuning [71] and network target assignment and positive/negative examples mining adapter [72], [73] in transfer pre-trained models to down- at the training stage, but it also involves the scale problem.
stream tasks, we propose two simple yet effective strategies Specifically, the widely used smooth L 1 loss tends to be a for language feature integration: (i) language prompter, and large number when we try to predict a large box, while (ii) language adapter. tends to be small when we try to predict a small one, even Language Prompter. We illustrates the architecture of a if their predictions have similar relative errors.
language conditioned vision encoder layer with language To address this problem, we normalize the coordinates prompter in Fig. 4(i). In this strategy, we first feed the of the ground-truth box by the scale of the image, and language tokens into a feed forward network (FFN) to gen- involve the generalized Io U loss [74] (GI o U loss), which is erate language prompt tokens. After that, language prompt not affected by the scales. Let us denote the prediction as tokens are concatenated with the [REG] token and vision b = (x,y,w,h), and the normalized ground-truth box as ˆ ˆ tokens to construct the input of a multi-head self-attention b = (xˆ,yˆ,wˆ,h). The training objective of our method is: (MHSA) module. The tokens out of MHSA module is split ˆ ˆ L = L (b,b) + L (b,b), (8) into two groups, i.e. one group for vision tokens and [REG] smooth-l 1 giou token, and another group for prompt tokens. The prompt where L (·) and L (·) are the smooth L 1 loss and smooth-l 1 giou tokens are dropped, while the other group is fed into the GI o U loss, respectively.
following FFN to obtain the input of next layers. Here, the parameters of the FFN to generate prompt tokens are 4 EXPERIMENTS randomly initialized, and those of other compnents make In this section, we present extensive experiments to validate use of parameters from the corresponding original vision the merits of our proposed Trans VG/Trans VG++ frameencoder layer of a pre-trained Vi T backbone network.
work. In Sec. 4.1 and Sec. 4.2, we first give a brief intro Language Adapter. The architecture of a language conduction to the datasets and experimental setup. In Sec 4.3, ditioned vision encoder layer with language adapter is ilwe conduct extensive ablative experiments to investigate lustrated in Fig. 4(ii). In this strategy, we inject the language the effectiveness of each component in our preliminary and expression information by adding an extra multi-head crossadvanced frameworks. Then, in Sec. 4.4, we present the attention (MHCA) module between the MHSA module main results of our models, and compare them with other and FFN of the original vision encoder layer. The query state-of-the-art methods. After that, in Sec. 4.5, we discuss embedding of this MHCA module is the output of MHSA the model size and computation costs of Trans VG++, and module, and both the key and value embedding are from the point out future directions to make it more applicable on language tokens. By this way, the outputs of MHCA modresource-limited devices. Finally, in Sec. 4.6, we show some ule, namely language adapt tokens, are the aggregation of qualitative results to help validation and analysis.
language features with adaptive weights against each vision token. The language adapt tokens are then elementwisely 4.1 Datasets and Evaluation Metric added to the vision tokens for vision-language feature fusion. Similar to the language prompter, the parameters of We conduct experiments on five prevalent datasets and the MHCA module are randomly initialized, and those of follow the standard protocol to evaluate our framework other parts are initialized with corresponding parameters in terms of accuracy. Specifically, only when the Jaccard from a pre-trained Vi T backbone network. overlap between the predicted region and the ground-truth By capitalizing on either a language prompter or a lan- region is above 0.5, the prediction is regarded as a correct guage adapter, the vision encoder layer can be converted one. These five datasets are detailed as follows: to language conditioned vision encoder layers, and thus Ref COCO [17] includes 19,994 images with 50,000 reenables it to be re-used for absorbing text expression infor- ferred objects. In each image, there are multiple instances mation from language tokens. The proposed LV i T maintains belonging to the same categories. Each referred instance has the original architecture of a Vi T model, so that we can more than one referring expression, and there are 142,210 naturally take advantage of pre-trained Vi T model to ease referring expressions in total. The samples in Ref COCO are the optimization for the whole framework. Particularly, officially split into a train set with 120,624 expressions, a both of these two strategies introduce negligible model pa- validation set with 10,834 expressions, a test A set with 5,657 rameters and computation costs compared with the whole expressions and a test B set with 5,095 expressions.
model, while achieving infusive performance improvements Ref COCO+ [17] contains 19,992 images with 49,856 reagainst our preliminary version with stand-alone fusion ferred objects and 141,564 referring expressions. Compared Transformers. In our experiments, we conduct extensive with Ref COCO, the words indicate the absolute position, ablative studies and analyses on these two strategies, and like ’left’ and ’right’, are not included in the language empirically show that using language adapter can achieve a expressions from this dataset. It is also officially split into slightly better accuracy among these two strategies. a train set with 120,191 expressions, a validation set with 10,758 expressions, a test A set with 5,726 expressions and a 3.4 Training Objective test B set with 4,889 expressions.
Unlike many previous methods that ground referred objects Ref COCO g [18] has 25,799 images with 49,856 referred based on a set of candidates (i.e., region proposals in two- objects and expressions. In this dataset, each image contains IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 8 2 to 4 instances for the referred categories, and each instance of multi-head attention module is 3, the hidden dimension are with an area more than 5% of the whole image. There in FFN is 768. For Vi T-small, the embedding dimension is are two commonly used split protocols for this dataset. 384, the head number of multi-head attention module is 6, One is Ref COCO g-google [18], and the other is Ref COCO g- the hidden dimension in FFN is 1536. For Vi T-small, the umd [1]. We report our performance on both Ref COCO g- embedding dimension is 768, the head number of multigoogle (val-g) and Ref COCO g-umd (val-u and test-u) to head attention module is 12, the hidden dimension in FFN make comprehensive comparisons. is 3072. No matter what Vi T model is leveraged, the hidden Refer It Game [19] includes 20,000 images collected from dimension in regression MLP is kept unchanged (i.e., 256 the SAIAPR-12 dataset [75], and each image has one or a as that in preliminary version) to eliminate the influence of few regions with corresponding referring expressions. This feature dimension in prediction head.
dataset is divided into three subsets, i.e., a train set with Training and Inference Details. Our model is end-to-end 54,127 referring expressions, a validation set with 5,842 optimized with Adam W optimizer, with batch size set as referring expressions and a test set with 60,103 referring 32. We apply several widely adopted data augmentation expressions. We use the validation set for ablation studies techniques [6], [7], [11], [21], [77] at the training stage. In and compare our method with others on the test set. Trans VG, we set the initial learning rate of fusion module Flickr 30 K Entities [20] is built on the original Flickr 30 K and prediction head to 10−4 and that of the vision branch [76], through introducing short region phrase correspon- and language branch to 10−5 . The weight decay is 10−4 . On dence annotations. It contains 31,783 images with 427 K Ref COCO, Ref COCO g and Refer It Game datasets, Trans VG referred entities. We follow the previous works [2], [11], [20], is trained for 90 epochs with a learning rate dropped by a [30] to separate the these images into 29,783 for training, factor of 10 after 60 epochs. On Flickr 30 K Entities, Trans VG 1000 for validation, and 1000 for testing. is trained for 60 epochs, with a learning rate dropping after 40 epochs. On Ref COCO+, Trans VG is trained for 4.2 Experimental Setup 180 epochs, with a learning rate drops after 120 epochs.
Inputs. The input image size is set as 640 × 640. When For Trans VG++, we set the initial learning rate of language performing image resizing, we keep the original aspect ratio branch and the parameters initialized from the pre-trained of each image. The longer edge of an image is resized to Vi T model to 10−5 , and that of other parameters to 10−4 .
640, while the shorter one is padded to 640 with the mean Thanks to removing the stand-alone fusion Transformer, the value of RGB channels. The maximum length of language total training epochs of Trans VG++ is reduced to 60 epochs.
expressions is set as 40 for Ref COCO g, and set as 20 for The learning rate drops by 10 times since the 45-th epoch.
other datasets. We cut off the language query if its length We follow the common practice [7], [11], [21], [77], [78] to iniis longer than maximum length (leaving one position for tialize the parameters of language branch with a pre-trained the [CLS] token and one position for the [SEP] token). BERT model [55], and to initialize the parameters of vi BASE Otherwise, we pad empty tokens after [SEP] token to make sion branch with models trained on MSCOCO [79] (the overthe input length consistent in each batch. The padded tokens lapping images between the training set of MSCOCO and are recorded with a mask, and will not influence the output. validation/test sets of Ref COCO/Ref COCO+/Ref COCO g Network Architecture. In our preliminary framework, we datasets are removed when performing pre-training). We use Res Net-50 or Res Net-101 [67] as the convolutional back- use DETR model [46] for vision branch initialization in bone network. The output of Res Net is 32 times downsam- Trans VG, and instead use Mask R-CNN [80] in Trans VG++, pling against the input image. Transformer encoder layers since these two initialization options achieve comparable in the preliminary framework follow the post-normalization performance, while the training epochs of Mask R-CNN is structure. The embedding dimension is set as 256, the head remarkably less than DETR.
number of multi-head attention modules is set as 8, and the Since our proposed frameworks directly output the box hidden dimension in FFN is set as 2048. coordinates, there is no post-processing during inference.
In our advanced framework, the patch embedding layer 4.3 Ablative Experiments divides a 640 × 640 image into patches of equal size, each with 16×16 pixels. These patches are flattened into vectors 4.3.1 Ablation Studies on Trans VG and fed into a fully connected layer to generate the initial In this section, we conduct ablative experiments to invesstate of visual tokens. To encode position information into tigate the effectiveness of each component in our Trans VG visual tokens, learnable position embedding is added to framework. We exploit Res Net-50 as the backbone network each visual token. Pariticularly, the resolution of position of the vision branch. All of the compared models are trained embedding in the standard Vi T model for image classifi- for 90 epochs on Refer It Game [19] for fair comparison.
cation is 14 × 14, as the input size is only 224 × 224. To Design of [REG] Token’s Initial State. In Table 1, we report make the position embedding matches the input size of the abation study on how to obtain the initial state of [REG] our method, we follow [53] to resize it to 40 × 40 with token. Specifically, we compare our learnable embedding bicubic interpolation. We experiment with three represen- with five other options to generate the initial state of [REG] tative models of Vi T series, i.e., Vi T-tiny, Vi T-small and Vi T- token (i.e., the embedding g appended to vision embedding r base, which is for vision feature encoding. The configuration and language embedding as in Equation (7)). We detail these of LV i T varies according to the Vi T variety. The feature designs and analysis as follows: dimension of our language adapter and language prompter — Average pooled vision tokens. We perform average pooling changes according to the Vi T model. Specifically, for Vi T- over the tokens out of vision branch, and exploit the tiny, the embedding dimension is 192, the head number average-pooled embedding as the initial state.
IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 9 TABLE 1: Ablative experiments on the design of [REG] TABLE 3: Performance comparison between different stratetoken’s initial state in Trans VG. The evaluation metric is gies to perform vision-languge fusion. Two representative the accuracy (%) on Refer It Game validation and test set. We datasets, i.e., Refer It Game and Ref COCO g-google, are excompare our default strategy to exploit learnable embedding ploited for evaluation. We report the accuracy (%) on valias the initial state of [REG] token with other options that dation sets of these two datasets.
generate the initial state from vision/language tokens out of the corresponding branch.
Fusion Strategies Refer It Ref COCO g-g Preliminary: Initial State of [REG] Token Acc@val Acc@test 6 encoder layers 71.00 67.24 Average pooled vision tokens 71.37 69.27 Language Prompter: Max pooled vision tokens 70.91 69.11 MHSA 71.39 68.46 Average pooled language tokens 69.96 68.15 Encoder layer 71.38 69.07 Max pooled language tokens 70.37 68.46 Linear 71.62 69.09 Sharing with [CLS] token 70.84 69.01 FFN 71.42 69.44 Learnable embedding ∗ 72.50 69.76 Language Adapter: Average pooling 72.01 69.49 Max pooling 72.26 68.96 TABLE 2: Ablative experiments on the vision branch and lan- [CLS] only 72.79 69.54 guage branch in our Trans VG. The performance is evaluated MHCA∗ 73.17 70.60 on the test set of Refer It Game in terms of accuracy (%). “Tr.” represents Transformer.
before performing multi-modal fusion.
Vision Branch Language Branch Accuracy (%) w/o Tr. w/ Tr. w/o Tr. w/ Tr.
(cid:88) (cid:88) 64.24 4.3.2 Ablation Studies on Trans VG++ (cid:88) (cid:88) 66.78 (+3.54) (cid:88) (cid:88) In this section, we conduct ablation studies on Trans VG++.
68.48 (+4.24) (cid:88) (cid:88) 69.76 (+5.52) Since the language branch remains unchanged, these studies focus on analysing LV i T. All of the compared models exploit Vi T-tiny as the backbone network of LV i T, and are trained — Max pooled vision tokens. We perform max pooling over for 60 epochs for fair comparison.
the tokens out of vision branch, and exploit the max- Degisn of Fusion Strategies. In Table 3, We present ablation pooled embedding as the initial state. studies on the effectiveness of different vision-language — Average pooled language tokens. Similar to the first option, fusion strategies. These models are compared on the valibut using the language tokens. dation set of representative Refer It Game and Ref COCO g- — Average pooled language tokens. Similar to the second google datasets. Our preliminary fusion strategy is to conchoice, but using the language tokens. catenate vision tokens and language tokens together, and — Sharing with [CLS] token. We use the [CLS] token of fed them into 6 stand-alone Transformer encoder layers. By language embedding to play the role of [REG] token. leveraging this preliminary strategies, the model achieves In this setting, the [CLS] token out of the V-L module 71.00% accuracy and 67.24% accuracy on Refer It Game and is fed into the prediction head. Ref COCO g-google, respectively.
— Learnable embedding*. This is our default setting by ran- Generally speaking, models capitalizing on language domly initializing the [REG] token embedding at the prompters and language adapters, no matter what internal beginning of the training stage. And the parameters of structure is used, achieve better accuracy than the standthis embedding are optimized with the whole model. alone Transformer encoder layers, which demonstrates the Our proposed design to exploit a learnable embedding advantage of fusion in backbone against fusion with exterachieves 72.50% and 69.76% on the validation test set of nal modules. We experiment with four different types of Refer It Game, which is the best performance among all the language prompters to generate language prompt tokens, designs. Typically, the initial [REG] token of other designs including a simple linear layer, a feed forward network is either generated from vision or language tokens, which (FFN), a Transformer encoder layer, and a multi-head selfinvolves biases to the specific prior context of the corre- attention (MHSA) module. Language prompters with these sponding modality. In contrast, the learnable embedding four structures work comparably on Refer It Game dataset, tends to be more equitable and flexible when performing and the model with an FFN works the best on Ref COCO grelation reasoning in the vision-language Transformer. google, achieving 69.44% accuracy. In experiments with Design of Transformers in Each Branch. We study the role language adapters, we evaluate four alternative designs to of the Transformers in the vision branch and the language obtain language adapt tokens, i.e., average pooling over branch, and report the comparison in Table 2. The baseline language tokens, max pooling over language tokens, taking model without the uni-modal Transformer in both branch linear transformation over [CLS] token, and our default reports an accuracy of 64.24%. When we only attach Trans- setting with a multi-head cross-attention (MHCA) module.
former to each vision branch or language branch, an im- The former three designs can be regarded as the degenerprovement of 68.48% and 66.78% are achieved, respectively. ation of language adapters with MHCA. Specifically, avg With the complete architecture, the performance is further pooling sets all the attention weights equally, max pooling boosted to 69.76% on the Refer It test set. This result demon- select one token for each channel to set its weight as 1 and strates the essential of Transformers in the vision branch and that of others as 0, and [CLS] only sets attention weights language branch to capture intra-modality global context of [CLS] token as 1 and that of others as 0. These three IEEETRANS.ONPATTERNRECOGNITIONANDMACHINEINTELLIGENCE,VOL.X,NO.X,JUNE 2022 10 TABLE 4: Ablative experiments on the location of language TABLE 6: Ablative experiments on improvements from conditioned visual encoder layers. The performance is evalu- Trans VG to Trans VG++. Both accuracy on validation set of ated on Ref COCO g-google dataset in terms of accuracy (%). Ref COCO g-google and model size are compared.
We use Vi T-tiny backbone by default.
Model Size (M) Acc.
Vision Feature V-L Fusion Location Accuracy (%) Vision Fusion (%) First 4 layers 68.26 (a) R-50 + 6 enc. layers 6 enc. layers 30.18 7.52 66.35 Last 4 layers 70.53 (b) Vi T-tiny 6 enc. layers 5.53 7.52 67.24 Evenly 4 layers∗ 70.60 (c) R-50-DC 5+ 6 enc. layers 6 enc. layers 30.18 7.52 65.96 (d) R-50 + 6 enc. layers language adapter 31.87 1.50 68.72 (e) Vi T-tiny language adapter 5.53 0.57 70.60 TABLE 5: Ablative experiments on the number of language conditioned vision encoder layers. We use Vi T-tiny backbone by default, and evaluate these models on Ref COCO g-google.
accuracy. Since the language branch of all the models follow Number Accuracy (%) the same configuration, it is omitted in this table. For clearer 2 70.13 comparison, we split LV i T into Vi T and language adapters 3 70.34 in this table. These models are evaluated on the validation 4∗ 70.60 6 70.58 set of Ref COCO g-google [18] dataset.
12 70.30 Model (a) is the preliminary baseline, i.e., Trans VG. It exploits Res Net-50 and 6 following Transformer encoder layers for vision feature extraction and 6 stand-alone Transformer encoder layers for multi-modal fusion. In this model, designs achieve similar performance with using MHCA the vision branch has 30.18 M parameters and the visionmodule when processing language expressions with simple language fusion module has 7.52 M parameters. It achieves phrases like that in Refer It Game, while lags behind using 66.35% accuracy on the validation set of Ref COCO g-google.
MHCA module when it comes to complicated sentences as collected in Ref COCO g-google. Model (b) replaces Res Net-50 and following Transformer As empirically shown in the table, the best choice is to encoder layers in model (a) with Vi T-tiny, reducing the use language adapters with MHCA for language adapt to- parameters of vision feature extraction model to 5.53 M.
ken generation, which achieves 73.17% on Refer It Game and The model size of (b) is more than 5 times smaller than 70.60% on Ref COCO g-google, outperforming the prelimi- that of (a), while the accuracy of (a) achieves 67.24%, outnary strategy by 2.17% and 3.36%, respectively. We leverage performing (a) by 0.89%. Note that Vi T-tiny’s capability this setting as our default one, and report the performance for image classification and object detection lags behind of models with this setting in our main results. Res Net-50, which shows the inconsistency between vision Location of Language Conditioned Vision Encoder Layers. perception and vision-language understanding. Besides, the In Table 4, we study where language conditioned vision better performance of (b) demonstrates the advantage of encoder layers should be located (i.e., applying language fully Transformer-based framework.
adapters to which vision encoder layers). The results are Model (c) leverages dilation convolution [82] in the last evaluated on Ref COCO g-google dataset. Our default config- stage of Res Net-50 backbone. By setting the dilation ratio uration is to evenly place the language adapters at the 3 rd, as 2, the downsampling rate of Res Net-50 reduces from 32 6 th, 9 th and 12 th vision encoder layers. We compare two to 16. Therefore, the input vision tokens to the following different configurations to place the language adapters at Transformer encoder layers are with the same number as the first 4 encoder layers or the last 4 encoder layers, which that of patches for a Vi T backbone (i.e., 1600). As observed, attempts to perform multi-modal fusion at the early stage the performances drops to 65.96%, which demonstrates that or at the late stage, respectively. As shown in the table, our the performance difference between model (a) and model default setting ranks the first among these configurations. (b) is not caused by token numbers, but due to the powerful Number of Language Conditioned Vision Encoder Layers. Vi T backbone and fully Transformer-based architecture.
Table 5 presents our ablation studies on how many language Model (d) uses the same structure as model (a) for vision conditioned vision encoder layers should be exploited. feature extraction, but capitalizes on language adapters for When increasing the number of language conditioned vision multi-modal fusion. In this model, a language adapter is encoder layers from 2 to 4, the accuracy is consistently added to each Transformer encoder layer, converting the improved from 70.13% to 70.60%. The performance is not original 6 vision encoder layers for vision encoding to shown to benefit from more language conditioned vision language conditioned vision encoder layers. To match the encoder layers once the number achieves 4. Therefore, we feature dimension of Transformers in model (a), in the empirically set the default number as 4. language adapter, the embedding dimension is set to 256, and head number of cross-attention module is set to 8. The 4.3.3 Improvements from Trans VG to Trans VG++ performance of model (d) reaches 68.72%, outperforming In this section, we verify the improvements from Trans VG model (a) by 2.37% and involving significant less parameto Trans VG++, i.e., upgrading the vision branch to a fully ters. This result demonstrates the significance of removing Transformer-based architecture and removing the stand- stand-alone vision-language fusion blocks and instead enalone V-L fusion module. Table 6 details five varieties of abling fusion in the vision encoder, which makes the model architecture configures, together with the model size and easier for optimization.