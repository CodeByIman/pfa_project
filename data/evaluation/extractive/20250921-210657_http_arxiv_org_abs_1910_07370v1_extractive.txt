Title: Evolution of transfer learning in natural language processing
Authors: Aditya Malte, Pratik Ratadiya
Year: 2019
Paper ID: http://arxiv.org/abs/1910.07370v1
Link: http://arxiv.org/pdf/1910.07370v1

=== Structured Extractive Summary (used by Ollama) ===
Abstract—In this paper, we present a study of the recent summarization were achieved through models such as the advancements which have helped bring Transfer Learning to Transformer and Seq 2 Seq architectures. Classically, tasks in natural Transfer Learning and rapid improvements in the performance language processing have been performed through rule-based and statistical methodologies. We† feel it necessary to discuss these of natural languages these methods do not generalise well and concepts before moving further: failed to learn the nuances of language. Thus machine learning algorithms such as Naive Bayes and decision trees coupled with A. Eventually, with the advent of advanced recurrent neural network architectures such as the LSTM, we to predict the succeeding word given the previous words were able to achieve state-of-the-art performance in several of the sequence as context. This task requires the language natural language processing tasks such as text classification model(LM) to learn the nuances and inter-dependencies among and machine translation.

This survey represents the performance of language models. INTRODUCTION i 1 i−1 model to word w given w preceding words of the sei i−1 Natural Language processing - the science of how to quence.
make computers effectively process natural text- has recently A lower perplexity generally signals towards a better perwitnessed rapid advancements thanks to increased processing forming language model as it indicates a lower entropy in the power, data and better algorithms. Language modeling is used for tasks such as use cases such as opinion mining, conversational agents and next word prediction, text auto-completion and checking linmachine translation among others. Transfer Learning However, owing to the vast and heuristic nature of natural Traditionally, NLP models were trained after random initiallanguage, machine learning gained a stronger ground in perization of the model parameters(also called weights). Machine learning models such as SVM, Learning, a technique where a neural network is fine-tuned on Naive Bayes and random forests found use in tasks such as a specific task after being pre-trained on a general task allowed sentiment analysis, spam detection and hate speech detection.
deep learning models to converge faster and with relatively On the other hand, Natural Language Generation(NLG) tasks such as machine translation, question-answering, abstractive †Equal contribution of both the authors 9102 tc O 61 ]LC.sc[ 1 v 07370.0191:vi Xra lower requirements of fine-tuning data.

These models suffered from an inability to learning has been mainly associated with the fine-tuning of retain information throughout the sequence and treated each deep neural networks trained on the Image Net dataset [3] for input independently. Recurrent Neural Networks [4] or RNN s attempted to redress In this survey, we seek to discuss the recent strides made in this shortcoming by introducing loops within the network, thus Transfer learning, Language modeling and natural language allowing the retention of information.
generations through advancements in algorithms and techniques. This type of training is generally referred to as semi-supervised training where the neural network is first trained as a language model on a general dataset followed by supervised training on a labelled training dataset Fig. 2: Recurrent Neural Network [7] thus establishing a dependence of supervised fine-tuning on unsupervised language modeling. The paper is structured as follows- Section II of the pah = φ(Wh + Ux + b) (2) t t−1 t per elaborates on the various algorithms and architectures that have serve as a base on which more advanced models As shown in Fig.2 and the corresponding equation, the have been built upon. These t then goes on to discuss the evolution of language modeling weights of the network are then updated through a training and transfer learning through models such as BERT, El Mo, algorithm called Backpropagation Through Time(BPTT) [5].

We conclude our survey and suggest future BPTT is, in essence, the backpropagation algorithm with improvements in section V. some modifications. Further, they suffered from Fig. 1: Developments in Transfer Learning and Language various issues while training such as the vanishing gradient Modeling problem (the error gradient being used for weight updation reducing to very low values) and the exploding gradient prob II. Machine learning models have been widely been used for an B. Long Short Term Memory array of supervised as well as unsupervised learning tasks such as regression, classification, clustering and recommendation The problem of ’long-term’ dependencies faced by earlier modelling.

Markov models such as the Multi-layer Perceptron recurrent neural networks was solved by designing a special Network, vector machines and logistic regression, however, kind of RNN architecture called the LSTM(Long Short Term did not perform well in sequence modeling tasks such as Memory) [6]. They were designed to keep track of information text classification, language modeling and tasks based on time for the extended number of timesteps. Gated Recurrent Units Gated Recurrent Units [8] or GRU s introduced by Cho, et al.
in 2014 are a curtailed variation of LSTM s designed to reduce the computation issues of the latter. The output to be forwarded by the node is decided by combining the values of current cell state with the results of the ’output layer’.

The performance is almost on par with LSTM but with C(cid:101) = tanh(W .[h ,x ] + b ) (5) t c t−1 t C efficient computation. However, with large data LSTM s with C = f ∗ C + i ∗ C(cid:101) (6) higher expressiveness may lead to better results.
t t t−1 t t D. While giving significant Dropconnect Algorithm Neural networks, prone to overfitimprovements in the domains of neural machine translation, ting, traditionally utilised Dropout as regularization to prevent encoding the context of complex and long sequences into overfitting. Dropout, an algorithm that randomly(with a proba single vector impeded the performance of the network.
ability p) ignore units’ activations during the training phase This was since a fixed-length vector was often incapable allows for the regularization of a neural network.

By diminishof effectively encoding the context of the given sequence.
ing the probability of neurons developing inter-dependencies, Consequently, this led to the birth of the Attention Mechanism, it increases the individual power of a neuron and thus reduces a novel technique that allowed the neural network to identify overfitting. However, dropout has not been able to provide which input tokens are relevant to a corresponding target token commensurate results in case of the RNN architectures. Instead of encoding a single vector to represent the se To this end, the drop connect algorithm randomly drops quence, the attention mechanism [11] computes a context weights instead of neuron activations. These weights are then used to perform a weighted Non-monotonically Triggered ASGD (NT-ASGD) sum of the encoder’s hidden states, thus obtaining the Context Stochastic gradient descent has been demonstrated to offer Vector c .
good performance for language modeling tasks through saddle t point avoidance and linear convergence.

Consequently, the Owing to the significant improvements gained due to authors found that keeping n = 5 provided good performance the Attention mechanism, Vaswani et. al. [12] proposed the in general. Better results were achieved compared to SGD Transformer architecture. The Transformer achieved new while training their model.
state-of-the-art results in various tasks such as machine E. Thus we are effectively able to encode the results of these operations are then concatenated. The ULMF it method the number of iterations that the LR is going to be achieved good results by using the then state-of-the-art AWD decreased or has been decreased.

The proposed network was a simdefines eta /eta and cut is the cutoff iteration min max ple 3-layer neural network without any attention mechanism, where the model switches from an increasing LR to a skip connections etc. The authors then proposed two novel methods- Discriminative Fine-tuning and Slanted h = concatenate(h ,maxpool(H),averagepool(H)) c T Triangular Learning rates to perform this task. ELMO is then concatenated with k the predictions given by both the Language Models. it and the obtained representations are then forwarded to the One can apply transfer learning using ULMF it by using pre- task-specific architecture. A pretrained bi LM can be used to trained models trained on datasets such as the Wikitext 103 obtain representations for any tasks and with fine-tuning, they data.

Peters et. al came up with a new word representation ELM o provides an enhancement to the traditional word called ”Embeddings from Language Models(ELM o)”, [14] embedding techniques by incorporating context while generin which the tokens beings assigned to each word were ating the same. These representations are more elaborate as they are word embeddings is also done to provide better performance.
dependent on all of the internal layers of the bi LM. Radford et al. at Open AI proposed a likelihood of both the previous words(forward LM) as well as semi-supervised approach called Generative Pre-training(GPT) the future words(backward LM) and maximizing it. [15] which involved unsupervised pre-tuning of the model and then task-specific supervised fine-tuning for language N (cid:88) →− understanding tasks. The ELM o model uses the intermedimulti-headed self-attention mechanism.
ate layer representation of the bi LM.

A single model is able to produce where sentences A and B are two sentences after performing quality results with minimum task-specific customization or the masking operations. This to obtain a fixed vector representation that is consequently architecture model was able to outperform other approaches used for classification and [SEP] is used to separate the two on tasks like natural language inference, question answering, input sequence. The authors were able to achieve an impressive sentence similarity etc. accuracy of 97-98% in the next sentence prediction task. To this end, the authors trained tuned on a relatively small dataset and requires lesser protheir model on two unsupervised prediction tasks: cessing power.

Classification can be performed by adding layers Replace the word with [MASK] token 80% of the time • to the [CLS Replace the word with another random word 10% of time • Question Answering Tasks • Keep the word as it is 10% of the time • Single Sentence Tagging Tasks • Performing prediction on only 15% of all words instead of Subsequently, two multilingual BERT models-uncased and performing prediction on all words would entail that BERT cased-for over 102 languages were released. Next Sentence Prediction This task entails predicting The amount of supervised training data present for language whether the first sequence provided immediately precedes the processing tasks is limited. Daniel et al.
Language Inference that involve understanding the relationship [18] proposed a new approach which involved direct encoding between two input sequences. Transformer-XL challenges of training transfer learning models wiz. complexity The Transformer-XL [20]was able to model very long-range and accuracy.
dependencies.

In essence a vanilla Transformer, it relied on model is trained over multiple tasks to make it as general two novel techniques-Recurrence mechanism and Positional as possible. This model achieves superior accuracy over the Encoding- to provide the improvement.
other architecture but at the expense of increased computation 1) Recurrence mechanism: Instead of training the Transrequirement and complexity. former on individual segments of the corpus(without regards to previous context), the authors propose caching the hidden 2) Deep Averaging Network(DAN) architecture: In this sequence state computed from the previous segments. Conmodel, the input embeddings for words and bi-grams are aversequently, the model computes self attention(and other operaaged and then passed through a deep neural network. The universal senthe absolute position can be recovered recursively.
tence encoder can be used for a variety of transfer tasks includ The Transformer-XL was able to surpass the state-of-the-art ing sentiment analysis, sentence classification, text similarity results for language modelling tasks on the enwiki 8 and text 8 etc.

The BERT model proposed by Authors et. al, was an Auto Encoding(AE) model that suffered from the following u.v problems: sim(u,v) = (1 − arccos( )/π) (29) ||u||||v|| The use of [MASK] tokens during the pre-training phase • led to a discrepancy as these tokens were absent during The sentence embeddings outperform the results of word the fine-tuning phase.
embeddings on the fore mentioned tasks. However, combining The model neglected inherent dependencies among two • word and sentence embeddings for transfer learning produced or more [MASK] tokens, thus leading to sub-optimal the best overall results. Universal sentence encoder assists the performance.
most when limited training data is available for the transfer A new model, the XLN et [21] was able to overcome these task.
difficulties by using a modification of general autoregressive The Universal Sentence Encoder can be used for down- pretraining.
stream tasks bypassing the generated embedding to a classifier Generalized Autoregressive Pretraining Phase Instead such as an SVM or another deep neural network. of using unidirectional language modeling or bidirectional masked language model to predict tokens, the paper proposed by Watching Movies and Reading Books”, ar Xiv:1506.06724 passing all permutations of a given sequence to the model [cs.CV] and predicting a particular token missing from this sequence.
[2] Ciprian Chelba, Tomas Mikolov, Mike Schuster, Qi Ge, Thorsten Despite random re-ordering of the sequence, order-related Brants, Phillipp Koehn, Tony Robinson, ”One Billion Word information remains preserved as positional encodings of the Benchmark for Measuring Progress in Statistical Language tokens remained the same for all permutations of the input Modeling”, ar Xiv:1312.3005 [cs.CL] sequence.
[3] Jia Deng, Wei Dong, Richard Socher, Li-Jia Li, Kai Li, Use of this modified form of pretraining helped overcome Li Fei-Fei, ”Image Net: A large-scale hierarchical image the two main challenges posed by the BERT architecture.
database”, 2009 IEEE Conference on Computer Vision and Along with that, the XLN et incorporated Transformer-XL into Pattern Recognition its core architecture. Lipton, John Berkowitz, Charles Elkan, ”A Critical Review of Recurrent Neural Networks for Sequence Learning”, two major modifications, the XLN et provided new state-ofar Xiv:1506.00019 [cs.LG] the-art results in 18 natural language processing tasks.

Lighter models such as the Contextualized Word Representations”, Volume: Proceedings Distil BERT and ALBERT are a step in the right direction and of the 2018 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language could potentially help bridge the gap between performance Technologies, Volume 1, June 2018 and processing power.

Key sentences:
- Abstract—In this paper, we present a study of the recent summarization were achieved through models such as the advancements which have helped bring Transfer Learning to Transformer and Seq 2 Seq architectures.
- Classically, tasks in natural Transfer Learning and rapid improvements in the performance language processing have been performed through rule-based and statistical methodologies.
- We† feel it necessary to discuss these of natural languages these methods do not generalise well and concepts before moving further: failed to learn the nuances of language.
- Thus machine learning algorithms such as Naive Bayes and decision trees coupled with A.
- Eventually, with the advent of advanced recurrent neural network architectures such as the LSTM, we to predict the succeeding word given the previous words were able to achieve state-of-the-art performance in several of the sequence as context.
- This task requires the language natural language processing tasks such as text classification model(LM) to learn the nuances and inter-dependencies among and machine translation.
- This survey represents the performance of language models.
- INTRODUCTION i 1 i−1 model to word w given w preceding words of the sei i−1 Natural Language processing - the science of how to quence.
make computers effectively process natural text- has recently A lower perplexity generally signals towards a better perwitnessed rapid advancements thanks to increased processing forming language model as it indicates a lower entropy in the power, data and better algorithms.
- Language modeling is used for tasks such as use cases such as opinion mining, conversational agents and next word prediction, text auto-completion and checking linmachine translation among others.
- Transfer Learning However, owing to the vast and heuristic nature of natural Traditionally, NLP models were trained after random initiallanguage, machine learning gained a stronger ground in perization of the model parameters(also called weights).
- Machine learning models such as SVM, Learning, a technique where a neural network is fine-tuned on Naive Bayes and random forests found use in tasks such as a specific task after being pre-trained on a general task allowed sentiment analysis, spam detection and hate speech detection.
deep learning models to converge faster and with relatively On the other hand, Natural Language Generation(NLG) tasks such as machine translation, question-answering, abstractive †Equal contribution of both the authors 9102 tc O 61 ]LC.sc[ 1 v 07370.0191:vi Xra lower requirements of fine-tuning data.
- These models suffered from an inability to learning has been mainly associated with the fine-tuning of retain information throughout the sequence and treated each deep neural networks trained on the Image Net dataset [3] for input independently.
- Recurrent Neural Networks [4] or RNN s attempted to redress In this survey, we seek to discuss the recent strides made in this shortcoming by introducing loops within the network, thus Transfer learning, Language modeling and natural language allowing the retention of information.
generations through advancements in algorithms and techniques.
- This type of training is generally referred to as semi-supervised training where the neural network is first trained as a language model on a general dataset followed by supervised training on a labelled training dataset Fig. 2: Recurrent Neural Network [7] thus establishing a dependence of supervised fine-tuning on unsupervised language modeling.
- The paper is structured as follows- Section II of the pah = φ(Wh + Ux + b) (2) t t−1 t per elaborates on the various algorithms and architectures that have serve as a base on which more advanced models As shown in Fig.2 and the corresponding equation, the have been built upon.
- These t then goes on to discuss the evolution of language modeling weights of the network are then updated through a training and transfer learning through models such as BERT, El Mo, algorithm called Backpropagation Through Time(BPTT) [5].
- We conclude our survey and suggest future BPTT is, in essence, the backpropagation algorithm with improvements in section V. some modifications.
- Further, they suffered from Fig. 1: Developments in Transfer Learning and Language various issues while training such as the vanishing gradient Modeling problem (the error gradient being used for weight updation reducing to very low values) and the exploding gradient prob II.
- Machine learning models have been widely been used for an B.
- Long Short Term Memory array of supervised as well as unsupervised learning tasks such as regression, classification, clustering and recommendation The problem of ’long-term’ dependencies faced by earlier modelling.
- Markov models such as the Multi-layer Perceptron recurrent neural networks was solved by designing a special Network, vector machines and logistic regression, however, kind of RNN architecture called the LSTM(Long Short Term did not perform well in sequence modeling tasks such as Memory) [6].
- They were designed to keep track of information text classification, language modeling and tasks based on time for the extended number of timesteps.
- Gated Recurrent Units Gated Recurrent Units [8] or GRU s introduced by Cho, et al.
in 2014 are a curtailed variation of LSTM s designed to reduce the computation issues of the latter.
- The output to be forwarded by the node is decided by combining the values of current cell state with the results of the ’output layer’.
- The performance is almost on par with LSTM but with C(cid:101) = tanh(W .[h ,x ] + b ) (5) t c t−1 t C efficient computation.
- However, with large data LSTM s with C = f ∗ C + i ∗ C(cid:101) (6) higher expressiveness may lead to better results.
t t t−1 t t D.
- While giving significant Dropconnect Algorithm Neural networks, prone to overfitimprovements in the domains of neural machine translation, ting, traditionally utilised Dropout as regularization to prevent encoding the context of complex and long sequences into overfitting.
- Dropout, an algorithm that randomly(with a proba single vector impeded the performance of the network.
ability p) ignore units’ activations during the training phase This was since a fixed-length vector was often incapable allows for the regularization of a neural network.
- By diminishof effectively encoding the context of the given sequence.
ing the probability of neurons developing inter-dependencies, Consequently, this led to the birth of the Attention Mechanism, it increases the individual power of a neuron and thus reduces a novel technique that allowed the neural network to identify overfitting.
- However, dropout has not been able to provide which input tokens are relevant to a corresponding target token commensurate results in case of the RNN architectures.
- Instead of encoding a single vector to represent the se To this end, the drop connect algorithm randomly drops quence, the attention mechanism [11] computes a context weights instead of neuron activations.
- These weights are then used to perform a weighted Non-monotonically Triggered ASGD (NT-ASGD) sum of the encoder’s hidden states, thus obtaining the Context Stochastic gradient descent has been demonstrated to offer Vector c .
good performance for language modeling tasks through saddle t point avoidance and linear convergence.
- Consequently, the Owing to the significant improvements gained due to authors found that keeping n = 5 provided good performance the Attention mechanism, Vaswani et. al. [12] proposed the in general. Better results were achieved compared to SGD Transformer architecture.
- The Transformer achieved new while training their model.
state-of-the-art results in various tasks such as machine E.
- Thus we are effectively able to encode the results of these operations are then concatenated.
- The ULMF it method the number of iterations that the LR is going to be achieved good results by using the then state-of-the-art AWD decreased or has been decreased.
- The proposed network was a simdefines eta /eta and cut is the cutoff iteration min max ple 3-layer neural network without any attention mechanism, where the model switches from an increasing LR to a skip connections etc.
- The authors then proposed two novel methods- Discriminative Fine-tuning and Slanted h = concatenate(h ,maxpool(H),averagepool(H)) c T Triangular Learning rates to perform this task.
- ELMO is then concatenated with k the predictions given by both the Language Models. it and the obtained representations are then forwarded to the One can apply transfer learning using ULMF it by using pre- task-specific architecture.
- A pretrained bi LM can be used to trained models trained on datasets such as the Wikitext 103 obtain representations for any tasks and with fine-tuning, they data.
- Peters et. al came up with a new word representation ELM o provides an enhancement to the traditional word called ”Embeddings from Language Models(ELM o)”, [14] embedding techniques by incorporating context while generin which the tokens beings assigned to each word were ating the same.
- These representations are more elaborate as they are word embeddings is also done to provide better performance.
dependent on all of the internal layers of the bi LM.
- Radford et al. at Open AI proposed a likelihood of both the previous words(forward LM) as well as semi-supervised approach called Generative Pre-training(GPT) the future words(backward LM) and maximizing it. [15] which involved unsupervised pre-tuning of the model and then task-specific supervised fine-tuning for language N (cid:88) →− understanding tasks.
- The ELM o model uses the intermedimulti-headed self-attention mechanism.
ate layer representation of the bi LM.
- A single model is able to produce where sentences A and B are two sentences after performing quality results with minimum task-specific customization or the masking operations.
- This to obtain a fixed vector representation that is consequently architecture model was able to outperform other approaches used for classification and [SEP] is used to separate the two on tasks like natural language inference, question answering, input sequence.
- The authors were able to achieve an impressive sentence similarity etc. accuracy of 97-98% in the next sentence prediction task.
- To this end, the authors trained tuned on a relatively small dataset and requires lesser protheir model on two unsupervised prediction tasks: cessing power.
- Classification can be performed by adding layers Replace the word with [MASK] token 80% of the time • to the [CLS Replace the word with another random word 10% of time • Question Answering Tasks • Keep the word as it is 10% of the time • Single Sentence Tagging Tasks • Performing prediction on only 15% of all words instead of Subsequently, two multilingual BERT models-uncased and performing prediction on all words would entail that BERT cased-for over 102 languages were released.
- Next Sentence Prediction This task entails predicting The amount of supervised training data present for language whether the first sequence provided immediately precedes the processing tasks is limited.
- Daniel et al.
Language Inference that involve understanding the relationship [18] proposed a new approach which involved direct encoding between two input sequences.
- Transformer-XL challenges of training transfer learning models wiz. complexity The Transformer-XL [20]was able to model very long-range and accuracy.
dependencies.
- In essence a vanilla Transformer, it relied on model is trained over multiple tasks to make it as general two novel techniques-Recurrence mechanism and Positional as possible.
- This model achieves superior accuracy over the Encoding- to provide the improvement.
other architecture but at the expense of increased computation 1) Recurrence mechanism: Instead of training the Transrequirement and complexity. former on individual segments of the corpus(without regards to previous context), the authors propose caching the hidden 2) Deep Averaging Network(DAN) architecture: In this sequence state computed from the previous segments.
- Conmodel, the input embeddings for words and bi-grams are aversequently, the model computes self attention(and other operaaged and then passed through a deep neural network.
- The universal senthe absolute position can be recovered recursively.
tence encoder can be used for a variety of transfer tasks includ The Transformer-XL was able to surpass the state-of-the-art ing sentiment analysis, sentence classification, text similarity results for language modelling tasks on the enwiki 8 and text 8 etc.
- The BERT model proposed by Authors et. al, was an Auto Encoding(AE) model that suffered from the following u.v problems: sim(u,v) = (1 − arccos( )/π) (29) ||u||||v|| The use of [MASK] tokens during the pre-training phase • led to a discrepancy as these tokens were absent during The sentence embeddings outperform the results of word the fine-tuning phase.
embeddings on the fore mentioned tasks.
- However, combining The model neglected inherent dependencies among two • word and sentence embeddings for transfer learning produced or more [MASK] tokens, thus leading to sub-optimal the best overall results.
- Universal sentence encoder assists the performance.
most when limited training data is available for the transfer A new model, the XLN et [21] was able to overcome these task.
difficulties by using a modification of general autoregressive The Universal Sentence Encoder can be used for down- pretraining.
stream tasks bypassing the generated embedding to a classifier Generalized Autoregressive Pretraining Phase Instead such as an SVM or another deep neural network. of using unidirectional language modeling or bidirectional masked language model to predict tokens, the paper proposed by Watching Movies and Reading Books”, ar Xiv:1506.06724 passing all permutations of a given sequence to the model [cs.CV] and predicting a particular token missing from this sequence.
[2] Ciprian Chelba, Tomas Mikolov, Mike Schuster, Qi Ge, Thorsten Despite random re-ordering of the sequence, order-related Brants, Phillipp Koehn, Tony Robinson, ”One Billion Word information remains preserved as positional encodings of the Benchmark for Measuring Progress in Statistical Language tokens remained the same for all permutations of the input Modeling”, ar Xiv:1312.3005 [cs.CL] sequence.
[3] Jia Deng, Wei Dong, Richard Socher, Li-Jia Li, Kai Li, Use of this modified form of pretraining helped overcome Li Fei-Fei, ”Image Net: A large-scale hierarchical image the two main challenges posed by the BERT architecture.
database”, 2009 IEEE Conference on Computer Vision and Along with that, the XLN et incorporated Transformer-XL into Pattern Recognition its core architecture.
- Lipton, John Berkowitz, Charles Elkan, ”A Critical Review of Recurrent Neural Networks for Sequence Learning”, two major modifications, the XLN et provided new state-ofar Xiv:1506.00019 [cs.LG] the-art results in 18 natural language processing tasks.
- Lighter models such as the Contextualized Word Representations”, Volume: Proceedings Distil BERT and ALBERT are a step in the right direction and of the 2018 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language could potentially help bridge the gap between performance Technologies, Volume 1, June 2018 and processing power.

Important terms: method, approach, algorithm, technique, model, analysis, study, experiment, results, propose, present, introduce, develop, design, machine learning, deep learning, neural network, classification, prediction, optimization, performance, accuracy
Focus: Methodological approach
Contribution: Introduces novel approach
