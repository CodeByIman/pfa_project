You are an expert research paper summarizer. Write a comprehensive, coherent summary of this research paper using ONLY the information provided below.

PAPER DETAILS:
Title: Graph Transform Optimization with Application to Image Compression
Authors: Giulia Fracastoro, Dorina Thanou, Pascal Frossard
Year: 2017


USER QUERY CONTEXT: processing with transformers
Make sure to address aspects relevant to this query.


REQUIREMENTS:
- Write 4-6 complete paragraphs (not bullet points)
- Start with: "This paper..."
- Cover: problem/context, methodology, results, and implications
- Use clear, academic language
- Stay strictly within the provided information
- Make it flow naturally as a cohesive narrative

EXTRACTED CONTENT TO SUMMARIZE (between <<< and >>>):
<<<
1 Graph Transform Optimization with Application to Image Compression Giulia Fracastoro, Dorina Thanou, and Pascal Frossard Abstract—In this paper, we propose a new graph-based trans- in the lowest frequencies. This provides a strong advantage form and illustrate its potential application to signal compression. compared to the classical DCT transform especially when the Our approach relies on the careful design of a graph that image contains arbitrarily shaped discontinuities. We introduce a novel graph contain many high frequency coefficients with high energy. estimation algorithm, which uncovers the connectivities between the graph signal values by taking into consideration the coding The GFT, on the other hand, may lead to sparse representations of both the signal and the graph topology in rate-distortion and eventually more efficient compression. terms. In particular, we introduce a novel coding solution for the However, one of the biggest challenges in graph-based graph by treating the edge weights as another graph signal that signal compression remains the design of the graph and the lies on the dual graph. A good graph for effective transform is introduced in the optimization problem by minimizing the sparsity of the coefficients of its graph Fourier transform (GFT) coding should lead to easily compressible signal coefficients, on the dual graph. The proposed technique is a general framework that they construct the graph by considering pairwise similarities can be applied to different types of signals, and we show two among pixel intensities ,  or using a lookup table that possible application fields, namely natural image coding and piecewise smooth image coding. The experimental results show stores the most popular GFT s .

It has been shown that that the proposed graph-based transform outperforms classical these methods could provide a significant gain in the coding fixed transforms such as DCT for both natural and piecewise of piecewise smooth images. In the case of depth map coding, the obtained images, the cost required to describe the graph often outweighs results are even comparable to the state-of-the-art graph-based the coding gain provided by the adaptive graph transform, and coding method, that are specifically designed for depth map images. often leads to unsatisfactory results. The problem of designing a graph transform stays critical and may actually represent the major obstacle towards effective compression of signals that I. In the last years, the new field of signal processing on In this work, we propose a new method for designing a graphs has gained increasing attention . The choice of the flexible graph signal representations, opening the door to new graph is thus posed as a rate-distortion optimization problem. structure-aware transform coding techniques, and eventually The cost of coding the signal is captured by minimizing the to more efficient signal and image compression frameworks. smoothness of the graph signal on the adapted graph. The solution of our optia flexible representation permits to go beyond traditional mization problem is a graph that provides an effective tradeoff transform coding by moving from classical fixed transforms between the sparsity of the signal transform coefficients and such as the discrete cosine transform (DCT)  to graphthe graph coding cost. based transforms that are better adapted to the actual signal We apply our method to two different types of signals, structure, such as the graph Fourier transform (GFT) . namely natural images and piecewise smooth images. Exper Hence, it is possible to obtain a more compact representation imental results on natural images confirm that the proposed of an image, as the energy of the image signal is concentrated algorithm can efficiently infer meaningful graph topologies, which eventually lead to a graph transform that can out G.

D.Thanouiswiththe Swiss Data Science Center,EPFL/ETHZ,Switzerland Moreover, we show that our method can significantly improve (e-mail: dorina.thanou@epfl.ch). the classical DCT on piecewise smooth images, and it even P. Graph-based image coding dedicated algorithms, it is important to underline that our In the last years, graph signal processing has been applied to framework is quite generic and can be applied to very different different image coding applications, especially for piecewise types of signals. smooth images. In , , the authors propose a graph-based Our work extends our previous study , where we have coding method where the graph is defined by considering shown that an initial version of our graph compression scheme pairwise similarities among pixel intensities. In this paper we show that the proposed method is been proposed in , where the authors use a lookup table highly general and it can be adapted to different types of that stores the most popular graphs. The signed graph Fourier transform, has been presented in . extension proposed in this paper offers a complete description This transform is targeted for compression of depth images of our novel system and a comprehensive performance analysis and its underlying graph contains negative edges that describe with extensive comparisons for various transforms. negative correlations between pixel pairs. We then introduce some preliminary proach have also been proposed for transform coding of inter definitions on graphs in Section III.

The appli- novel graph-based method for intra-frame coding has been cation of the proposed graph construction algorithm to image presented in , which introduces a new generalized graph coding and the entire compression framework are described Fourier transform. Then, the experimental results on natural images video coding has been introduced in , where the authors and piecewise smooth images are presented in Section VI and design a set of simplified graph templates capturing the basic VII, respectively. Another graph-based separable transform for inter methods, that are closely related to the framework proposed predictive video coding has been presented in . In this case, in this work. the proposed transform, called symmetric line graph transform, has symmetric eigenvectors and therefore it can be efficiently implemented. Transform coding Finally, a few graph-based methods have also been presented for natural image compression. In , a new tech Lossy image compression usually employs a 2 D transform nique of graph construction targeted for image compression to produce a new image representation that lies in the transis proposed.

Usually, the obtained transform coefficients quantization and edge prediction technique. Finally, a method for designing signal that has Gaussian entries . Nevertheless, this model fails to capture the complex and nonstationary behavior that is typically present in natural Several attempts to learn the structure and in particular a images. Moreover, we show how xˆ = ΨTx. our generic framework can be used for image compression. In order to find the GFT It is important to underline that the graph Laplacian obtained that best approximates the KLT, we introduce a maximum using (1) is independent from the edge orientation in G. likelihood estimation problem, using an approach similar to the one presented in .

For this reason, in (6) is reduced to finding the graph that permits to minimize the next section we present a new graph learning method the rate terms. that considers also this auxiliary cost and we highlight the connection between the proposed graph construction problem B. GRAPH TRANSFORM OPTIMIZATION , namely Graph-based compression methods use a graph representa-     N−1 tion of the signal through its GFT, in order to obtain a data-   R (L) = u TL u = u T λ (L)ψ (L)ψ (L)T u c l l l adaptive transform that captures the main characteristics of the l=0 signals. Thus, (7) does not 0 and its coding costs, we introduce a new graph construction capture the variable cost of DC coefficients in cases where the approach that takes into consideration the above mentioned graph contains a variable number of connected components. criteria. For this reason, we can say that the solution c composed of two representation costs, namely the cost of the of our optimization problem can be seen as an approximation signal transform coefficients R (L) and the cost of the graph of the KLT. c 5 G with this purpose. (a) (b) In particular, in  the authors propose a method for joint denoising and contrast enhancement of images using the graph Fig. 1.

Graph construction problem a graph G, we define its dual graph G as an unweighted graph d By using (1), (7) and (8), our graph construction problem where each node of G represents an edge of G and two nodes d (6) is reduced to the following optimization problem of G are connected if and only if their corresponding edges d in G share a common endpoint. In this way, the dual graph the graph by solving the following optimization problem can provide a smooth representation of w. Block diagram of the proposed coding method for an input image u. where tr(·) denotes the trace of a matrix, vec(·) is the vector- and the reduced vector wˆ∗, then we quantize wˆ∗ and code r r ization operator, and M ∈ RM 2×M is a matrix that converts it using the entropy coder described above. Since it is important to find the best tradeoff between the (11) quality of the graph and its transmission cost, for each block in The problem in (11) is a convex problem with respect to the an image we test different quantization step sizes {∆ } i 1≤i≤Q variable w and can be solved via interior-point methods  for a given graph represented by wˆ∗.

It is important to underline that the main goal the distortion and the rate of the reconstructed image signal of this section is to present an application of our framework. obtained using the graph transform described by wˆ∗ . We r,∆ Therefore, we do not present an optimization of the full coding i point out that the choice of ∆ depends on the quantization i process, but we mainly focus on the transform block. step size q used for the image transform coefficients uˆ. Then, since The proposed image compression method requires to comwe assume that the non-zero coefficients are concentrated pute, both at the encoder and at the decoder, the eigenvectors in the low frequencies, we code the quantized coefficients of the Laplacian matrix. For this reason, the overall complexity of the proposed graph description, we reduce the number of elements in wˆ by image compression method is higher at the encoder than taking into account only the first M    M coefficients, which at the decoder.

In Table I we show the execution times of the r quantized using the same step size for all its coefficients and proposed method for encoding and decoding an image block. then coded with the same entropy coder used for the image We also perform a comparison with other three techniques, signal. namely the method proposed in , a baseline DCT coder Given an image signal, we first solve the optimization and a graph-based method where the graph is designed using problem in (11) obtaining the optimal solution w∗. For this DCT 6.95·10−4 s 6.15·10−4 s reason, we perform a priori a block classification using a  3.55·10−2 s 1.10·10−3 s structure tensor analysis, as done in . Instead, at the decoder we can see that the performance of the proposed method are comparable with • Class 1: smooth blocks, if μ 1 ≈ μ 2 ≈ 0; the ones of  and the Gaussian method. • Class 2: blocks with a dominant principal gradient, if μ   μ ≈ 0; 1 2 Class 3: blocks with a more complex structure, if μ and VI. EXPERIMENTAL RESULTS ON NATURAL IMAGES • 1 μ are both large.

2 In this section, we evaluate the performance of our illustra Fig. 3 shows an example of block classification. We first describe class, we have set the values of parameters α and β by fine the general experimental settings, then we present the obtained tuning. We set α = 100 for blocks that belong to th
>>>

Now write the comprehensive summary following the requirements above: