
You are given the full extractive summary of a research paper. Using ONLY the information contained in that extractive text, write a comprehensive, fluent narrative summary that:
- Contains 8 to 15 full sentences grouped into 2 to 4 paragraphs (no bullet points)
- Explains the problem/context, the approach/method, the main results/findings, and any limitations/implications
- Uses the terminology and details present in the extractive text (do not invent or add external facts)
- Starts with: "This paper ..."
- Do NOT copy equations, variable names, or proof snippets; instead paraphrase their meaning in plain academic language.

User is interested in: Deep learning. 

Paper: "Prediction Surface Uncertainty Quantification in Object Detection Models for Autonomous Driving" by Ferhat Ozgur Catak, Tao Yue, Shaukat Ali (2021)

EXTRACTIVE TEXT (entire content to use faithfully):
<<<EXTRACTIVE>>>
FOCUS: Methodological approach CONTRIBUTION: Introduces novel approach LINES COUNT: 58 TERMS: method, approach, algorithm, technique, model, analysis, study, evaluation, experiment, results

BODY: Prediction Surface Uncertainty Quantification in Object Detection Models for Autonomous Driving Ferhat Ozgur Catak Tao Yue Shaukat Ali Simula Research Laboratory Simula Research Laboratory Simula Research Laboratory Fornebu, Norway Nanjing University of Aeronautics and Astronautics Fornebu, Norway ozgur@simula.no taoyue@ieee.org shaukat@simula.no Abstract—Object detection in autonomous cars is commonly clustering method for linking these bounding box predictions. based on camera images and Lidar inputs, which are often used We modified three object detection DL models (Yo Lo, SSD 300 to train prediction models such as deep artificial neural networks and SSD 512) and conducted experiments with four realfor decision making for object recognition, adjusting speed, etc. The results of the statistical is vital to measure the reliability of decisions made by such prediction models via uncertainty measurement. Uncertainty, analysis showed a significant negative correlation between in deep learning models, is often measured for classification uncertainty and prediction accuracy. However, deep learning models in autonomous driving that autonomous driving’s decisions accuracy is negatively are often multi-output regression models. Hence, we propose a impacted by uncertainty, thus requiring new methods to deal novel method called PURE (Prediction s UR face unc Ertainty) for with uncertainty. We formulate the object recognition problem as a regression model The paper is organized as follows: Section II introduces the with more than one outputs for finding object locations in a related work. For evaluation, we modified three Section IV presents our method, followed by its evaluation in widely-applied object recognition models (i.e., Yo Lo, SSD 300 Section V. The Monte-Carlo (MC) dropout method is used in many autonomous driving machine learning problems such as regression, classification, and in different domains such as medical applications , au I. The MC dropout Deep Learning (DL) is increasingly used in autonomous method makes predictions for an input to measure uncertainty. Existing research focuses more on increasing the Bayesian Neural Network based MC dropout is the primary accuracy of a DL model but less on studying its uncertainty path to uncertainty quantification in DL , . However, its quantification is essential to improve the is Deep Ensembles, a non-Bayesian method for uncertainty prediction quality of the DL model. The ensemble can predict the same input instance of input data; thus, the model is uncertain because it does with different predictions using its individual models. As a result, the parameters of a neural network suitable for single model uncertainty quantification. Therefore, model are poorly optimized for unseen inputs in the prediction we opted for the Bayesian neural networks approach. We propose a new method called PURE (Prediction s UR- showed that a DNN model with prediction time dropout face unc Ertainty (PURE) to quantify prediction uncertainty of is equivalent to a specific variational inference on a Bayesian an DL-based object detection model using multiple predictions neural network model. The DL model makes multiple different approximated by averaging probabilistic feed-forward MC bounding box predictions for a given object. The approach is 1202 lu J 11 ]VC.sc[ 1 v 19940.7012:vi Xra highly effective for practical implementation of large models for each of its single ensemble model, the system drop outs and has proved useful in understanding dynamics of the different neurons in the network’s each layer according to the networks under different testing conditions .  studied how Bayesian Convolutional Neural Networks the average of the predictions over dropout iterations, T, and can quantify uncertainty in DNN models to enhance the is used as the final inference, yˆ, for the input instance x in diagnostic performance of the human-machine alliance using the dataset. Another study uses by finding the entropy and the probabilistic feed-forward uncertainty values and quality estimates to model predictive MC dropout sampling variance during prediction time. Their results number of predictions of the MC dropouts, and x is the input show that wrong angle decisions have significantly higher un- sample. But, models can be used to predict the input instance’s class label PURE employs existing multi-output DL models with cars’ and uncertainty quantification of the overall prediction. They DNN models are affected by epistemic and aleatoric employed density-based clustering algorithms such as HDB- uncertainties, as briefly explained in Section I. Epistemic SCAN, Basic Sequential Algorithmic Scheme, modified the uncertainty should be eliminated by taking additional training SSD model with new dropout layers, and measured object data and making optimal model configurations (i.e. Measurement errors (e.g., in sensor inputs) are There exist works – in modeling/measuring una common source of confusion in neural networks because certainty for supporting the testing of cyber-physical system there is no guarantee that the resulting prediction will be (CPS s). Object Detection 0.5 Object detection is an algorithmic approach in deep learning 0.0 related to computer vision and image processing that deals 0.5 with detecting a specific label’s objects in digital images and videos. Classical object 1.5 0 2 4 6 8 10 12 detection techniques use handcrafted features and lightweight x machine learning models .  (also see Section II) proposed an MC Dropout approach adding intermediate dropout layers to a model without changing the numbers of neurons and layers of existing object recognition models. Uncertainty Quantification Methods A typical DNN model predicts a single output for a regression problem or a softmax probability of each label for a classification problem. To quantify a single prediction’s uncertainty, the model should produce multiple outputs for the single input instance. For regression models, the only uncertainty detection predictions by quantifying each object’s uncertainty quantification metric is the variance of the predictions: in a camera scene. For standard deviation is the only quantification method for singlemulti-output regression problems, we need another approach. We propose prediction surface, a novel boundary. Moreover, the number of objects in a scene cannot uncertainty quantification method for multi-output regression be quantified easily, as each object cannot be detected in each models. In this approach, the object detection model makes Bayesian inference. T different predictions with the MC dropout method for the The MC dropout-based uncertainty quantification method input image X. For each predicted object, O , in the image makes multiple predictions with randomly dropped neurons i X, the predicted corners locations (x,y) are calculated using from the same model. To do so, we inject the prediction- Thus, the problems in object detection from car camera time activated dropout layers to the existing models to create views are: detecting the number of objects within all prediction small variations of the current predictive models to find the results in Bayesian Neural Networks, and quantifying predicoverall predictions’ uncertainty. Algorithms Algorithms 1-2 outline the top-level steps implemented in Trainingprocesswithpredictiontimeactivateddropouts PURE: 1) the pre-trained object detection model modified with prediction time activated dropout layers, and 2) density-based DBSCANC lustering Algorithm object recognition grouping from the modified object detection •tofindthenumberofclusters 1011 1011 1011 1011 •i.e.numberofobjectsfromthe cameraview model’s predictions with MC dropout. Zoom Intotheobjectprediction cluster(Pedestrian) Algorithm 1 takes the object detection model h as the input, Monte-Carlo Dropout Based Predictions lets it make T times MC dropout predictions for a given image, 1011 1011 1011 1011 1011 1011 and records all predictions and their centers. We use the DBSCAN clustering Machine learning models extract patterns from their training algorithm to find objects . Algorithm 1: MC dropout based object predictions Cam camera view Object detection predictions Input: X ∈ Rm×n: camera view h: Object detection model Corner-1: Corner-2: T: MC dropout size 429.47 442.88 Output: Predictions center lists center lists ← ∅ 1 /* T predictions using the object detection model h */ 2 foreach t ∈ T do /* Predict objects with surrounding boxes Corner-3: Corner-4: from image X */ 544.13 555.02 VB oxes ← h(X) 3 (a) High uncertainty 4 foreach box ∈ VB oxes do /* Get the corner locations of each Cam camera view Object detection predictions surrounding box */ x , , 5 1 1 x , 6 2 2 Corner-1: Corner-2: /* Find the width and height of the 274.46 246.22 predicted object */ width,height = x −x ,y −y 7 2 1 2 1 x ,y ← x width,y + height 8 center center 1 1 2 2 center lists ← Update Archive(x ,y ) 9 center center Corner-3: Corner-4: 296.95 275.66 10 return center lists (b) Low uncertainty Algorithm 2: DBSCAN clustering algorithm based Fig. Find object In this section, we first present the research questions (RQ s), clusters from the center l ist */ followed by the object detection models and datasets employed Clusters ← DBSCAN(center list, ) 2 3 foreach cluster c ∈ Clusters do in the experiment. We also present experiment metrics and /* Calculate the area of each corner of the procedure, followed by results and threats to validity. [RQ 2] Is there any object’s) uncertainty */ correlation between prediction surface uncertainty and the 8 area ← Update Archive(Mean(area x 1 , area y 1 , area x 2 , area y 2 )) object detection performance of DNN models? This RQ tests /* Average uncertainty of all detected objects the hypothesis that the prediction performance of a DNN from all T predictions */ model decreases with the increase in the uncertainty in images U ← Mean(area) 9 used for object detection. We modified them to ensure their compatibility with Figure 3 shows the low and high uncertain predictions from the MC dropout approach. We implemented PURE in Python with the Tensorflow deep learning library. Berkeley Deep Drive 50,004 1280×720 The worst results were obtained on the Berkeley dataset NEXET 44,527 1280×720 due to the reason that each picture in this dataset has too many objects leading to the low performance of the models. To inject new dropout layers, we need two components: the model structure in the Python language, Figure 5 shows five examples from each dataset. TABLE II: Object detection prediction results of all the models for all the datasets Model Dataset Avg. 2) RQ 2: Table III shows the experimental results for all the datasets with all the object detection models. We used the Pearson correlation method to find the correlation between prediction surface uncertainty and Io U . Thus, we can conclude that with the increase in uncertainty, the prediction accuracy of object detection in autonomous driving TABLE III: Results of the Pearson correlation coefficient between prediction surface uncertainty and Io U. For a better visualization, as an example, Figure 9 presents the scatter plots of the prediction surface uncertainty and object detection performance metric IOU, with 0.25 dropout ratio for the SSD 512 model. Our experimental results based Empirical Software Engineering, 2021.
<<<END>>>

Now produce the final summary as specified (8–15 sentences, 2–4 paragraphs, no bullets).