You are an expert research paper summarizer. Write a comprehensive, coherent summary of this research paper using ONLY the information provided below.

PAPER DETAILS:
Title: Joint Optimization of Primary and Secondary Transforms Using Rate-Distortion Optimized Transform Design
Authors: Darukeesan Pakiyarajah, Eduardo Pavez, Antonio Ortega...
Year: 2025


USER QUERY CONTEXT: processing with transformers
Make sure to address aspects relevant to this query.


REQUIREMENTS:
- Write 4-6 complete paragraphs (not bullet points)
- Start with: "This paper..."
- Cover: problem/context, methodology, results, and implications
- Use clear, academic language
- Stay strictly within the provided information
- Make it flow naturally as a cohesive narrative

EXTRACTED CONTENT TO SUMMARIZE (between <<< and >>>):
<<<
JOINT OPTIMIZATION OF PRIMARY AND SECONDARY TRANSFORMS USING RATE-DISTORTION OPTIMIZED TRANSFORM DESIGN Darukeesan Pakiyarajah⋆, Eduardo Pavez⋆, Antonio Ortega⋆, Debargha Mukherjee†, Onur Guleryuz†, Keng-Shih Lu†, Kruthika Koratti Sivakumar† ⋆University of Southern California, Los Angeles, CA, USA †Google LLC, Mountain View, CA, USA ABSTRACT Data-dependent transforms are increasingly being incorporated into DCT next-generation video coding systems such as AVM, a codec under DCT+ST 1 development by the Alliance for Open Media (AOM), and VVC. In this work, we propose an separable KLT+ST 3 optimizationframeworkforjointlydesigningprimaryandsecondary transforms from data through a rate-distortion optimized clustering. We empirically evaluate three primary transforms and three secondary transforms (ST): (a) our proposed approach using AVM residual data and demonstrate Tree-structured clustering and (b) Joint clustering with separable that 1) the joint clustering method achieves lower total RD cost path graph transform (SPGT). in the RDOT design framework, and 2) jointly optimized separable path-graph transforms (SPGT) provide better coding efficiency to refine the coding efficiency. These systems typically use a ratecompared to separable KLT s obtained from the same data. distortion optimized transform (RDOT) design approach [16–18], Index Terms— data-dependant primary transforms, secondary which aims to create data-dependent transforms that minimize ratetransforms, joint optimization, rate-distortion optimized transforms, distortion costs by using a clustering step to identify training data separable path graph transforms examples that cannot be well represented by the fixed transforms in the codec (e.g., DCT and ADST).

To address the limondary transform design restricts the data in each cluster to either itations of KLT, several alternative approaches have been proposed, use a secondary transform or not, leading to a suboptimal design including separable KLT s , graph-based separable transforms where the set of all primary and secondary transforms in the system (GBST) , and secondary transforms [7–13]. Secthatthese RDOT designframeworkstypicallyuseseparable KLT sas ondary transforms have been successfully integrated into VVC , primarytransforms,requiringthelearningof N 2/2 parametersfrom while GBST s and separable KLT s are being considered for nextthe data to construct an N × N transform matrix . Separable primary transforms, such as be scarce for learning a specific mode-dependent transform because separable KLT s and GBST s, are designed to be applied indepensome intra-prediction modes are infrequently used and, if multiple dently to the rows and columns of data blocks. We address these limitations by (1) proposing a joint rate 5202 ya M 12 ]VI.ssee[ 1 v 40151.5052:vi Xra distortion optimized clustering approach to learn primary and sec- Algorithm 1 RDOT Learning ondary transforms and (2) employing learned separable path graph Input: {X }M , C for j = 1,...,K i i=1 j transforms (SPGT) as primary transforms.

We are unaware of other Initialize: {S }K , {F }K j j=1 j j=1 work considering joint primary and secondary transform design and 1: repeat leveraging path-graph models for mode-dependent RDOT design. 2: update F ∈ C for j = 1,...,K j j     In our joint design framework, all the data is clustered directly 3: S = i : arg min d(X ,F ) + λB = j j i k i,k into six groups, allowing each data block to select the best combi- k for j = 1,...,K nation of primary and secondary transforms from all choices in the 4: until convergence system(see Fig.1 b). Let X  ̃ i,j denote the transform coeffimethods due to their robustness and ability to learn transforms from cients after applying the transform F j and d(X i ,F j ) represent the  ̃ limited data . Let S denote i j model choice reduces the number of parameters learned from data the index set of residual blocks that choose the transform F : j to N to design transforms for rows or columns with N pixels, com-     pared to N 2/2 in KLT-based methods.

This approach has been used to if n = 16, fewer than 128 parameters need to be learned due to design primary transforms based on separable KLT s  and secthe sparse structure of the covariance matrix. Weempiricallyevaluateourproposeddesignofmode-dependent primary and secondary transforms using intra-prediction residual 3. JOINT OPTIMIZATION OF PRIMARY AND data from AVM, a codec under development by the Alliance for SECONDARY TRANSFORMS Open Media (AOM). Our results demonstrate that the joint clustering approach with SPGT s achieves the lowest RD cost in transform For a block X , a separable transform can be defined using two ori design, outperforming existing tree-structured clustering methods thonormal N × N transforms, a column transform C and a row j with separable KLT s and providing an average bitrate saving of transform R .

Let x = vec(X ) represent the vector i,j j i j i i additionalimplementationcomplexity. To address this challenge, clustering framework, where sufficient training data is limited. in , the authors proposed using the l -norm of the quantized 0 coefficients, ∥yˆQ ∥ , which represents the number of non-zero coi,j 0 efficients, as a proxy for B in the RDOT framework. Secondary transform i,j same proxy in our joint learning framework, so the RD optimized Consider the case where the transform corresponding to the jth clusclustering in step 3 of Algorithm 1 becomes: ter is a primary transform followed by a secondary transform, with     G and P fixedapriori. We use an j that are used for the transform updates in step 2 of Algorithm 1: empirical approach to obtain P for each intra-prediction mode in j our setting.

EMPIRICAL RESULTS col row variance matrices as in separable KLT methods. We adopt a model basedonapathgraphwithaself-loopatthefirstnodefortheinverse Weuseintra-predictionresidualsobtainedfrom AVM forbothlearncovariance matrices of the rows and columns of {X } . First 16 columns (basis) of the transform F = G P T 6 SPGT 6 6 designed using the joint learning with SPGT for D 135 PRED (diagonal prediction), 8 × 8 residuals, reshaped into 8 × 8 blocks. tree-structured clustering of the same data, but with F obtained us 3 ingtheseparable KLT methodasdescribedin ,wherethe R and (a) (b) 3 C are obtained from the eigenbasis of S and S , the sample co 3 col row Fig. 4. The numbers 1–16 indi- Note that we have two clustering approaches, {Joint,Tree}, cate the coefficients selected for the secondary transform. and two transform learning methods to design primary transforms, {SPGT,sep.KLT}, with each combination of clustering and trans 4.1.

Mode-dependent RDOT learning form learning resulting in four methods to design RDOT s: (Joint, SPGT), (Joint, sep.KLT), (Tree, SPGT), and (Tree, sep.KLT). For We design data-dependent primary and secondary transforms for the our experiments, we consider training data consisting of 8 × 8 and 12 principal intra-prediction modes in AVM in a mode-dependent 16 × 16 residuals and train mode-dependent transforms separately manner. Furthermore, in RDOT design, we use QP = 28 with SPGT (Joint, SPGT). and λ = 0.85 × 2(QP−12)/3, which are typical values used for First, we show how the low-frequency coefficients are selected RDOT design for intra-prediction residuals in codecs like AVC for different intra-prediction modes for the subsequent secondary and HEVC . Examples of the 16 coefficients selected for two different transforms, we observed that the covariance matrices of the n low 8 × 8 intra-prediction modes of AVM are illustrated in Fig. 4. frequency transform coefficients are of full rank, substantiating that Moreover, to compare against the existing tree-structured clusnon-separable KLT s with fewer parameters can effectively learn the tering approaches , we design primary and secondary transsecondarytransforms.

Run three independent instances of Algorithm 1 to design designedusingthejointlearningmethodfor D 135 PRED(diagonal the secondary transforms with (i) {X } ,C ,C , (ii) prediction), 8×8 residuals are shown in Fig. 5. From the figure, we i i∈S 1 1 4 {X } ,C ,C , (iii) {X } ,C ,C as inputs to obtain observe that the low-frequency basis functions exhibit smooth varii i∈S 2 2 5 i i∈S 3 3 6 secondary transforms. ations in the direction of the corresponding intra-prediction mode, We refer to this method as tree-structured clustering with SPGT demonstrating that the combination of primary and secondary trans- (Tree, SPGT). Further, to commethod for designing primary transforms, we repeat the joint and pare these four learning methods, we compute the total RD cost ob Method DC V H D 45 D 135 D 113 D 157 D 203 D 67 S S V S H Tree, sep. For each method, including the baseline, we compute the bitrate and distortion for each block (b) and average the results across multiple blocks.

Significant gains in directional tained using these methods, given by prediction modes suggest that data-dependent transforms effectively K adapttotheirdirectionalstatistics. Furthermore, we present the BD-rate savings for all methods as prediction modes. The results for two of those modes are presented a function of the number of data samples used to train the RDOT s in in Fig. 3. CONCLUSION In this work, we proposed a method for simultaneously learning 4.2.

Testing data-dependent primary and secondary transforms using the rate Next, we demonstrate that the transforms designed using the distortion optimized transform design framework. To learn the sepjoint clustering method with SPGT provide better coding gains arable primary transforms, we utilized a path graph model with a than other methods in video coding applications. We empirically evalueach intra-prediction mode, we independently encode each resid- ated our approach using AVM intra-prediction residuals, comparual block. Our experimental results demon QP = {26,27,28,29,30,31}, and entropy coding the coefficients stratedthat SPGT soffersignificantgainsover KLT-basedmethodsin using a CAVLC encoder .

Then, we perform a secondary RDO step to on implementing and evaluating the proposed joint transform design decidewhetherornottoapplyasecondarytransform.
>>>

Now write the comprehensive summary following the requirements above: